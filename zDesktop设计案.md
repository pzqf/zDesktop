# zDesktop — 智能桌面管理系统 设计案

> **版本**: v2.3 | **日期**: 2026-07-23 | **状态**: 设计评审中
>
> **v2.3 变更**: 新增「交互流程设计」章节，定义全局导航体系、页面间跳转流程、页面内功能交互、核心用户路径。
>
> **v2.2 变更**: 暂缓剪贴板历史、音乐控制组件，相关功能移入"后续规划"；同步修正功能编号、架构图、技术选型、里程碑、竞品与风险表的一致性。
>
> **v2.1 变更**: 新增元气桌面竞品深度分析；基于分析结论修正桌面接管技术路线（放弃 WorkerW，改用独立透明窗口 + Z 序控制）；技术选型确定 WPF .NET 8 方向。
>
> **v2.0 变更**: 整合效率增强、交互体验、视觉个性化、系统深度整合四大亮点方向。

---

## 一、产品定位

zDesktop 是一款面向 Windows 平台的**智能桌面管理增强工具**，在保留原生桌面完整体验的基础上，提供文件智能分类、多窗格文件管理、桌面组件、任务栏增强、全局搜索、快速启动器、窗口管理、自动化规则、系统控制中心等能力，目标是让用户的桌面从"混乱的堆叠"变为"有序的指挥中心"。

**核心原则**: 替换原生桌面的视觉层，但不破坏原生桌面的任何交互能力——桌面图标可点击、任务栏可用、窗口切换正常、退出软件后桌面自动恢复。

---

## 二、功能模块设计

### A. 桌面管理与美化

#### 1. 智能文件分类 🧠

| 特性 | 说明 |
|------|------|
| **AI 识别引擎** | 基于文件扩展名 + 内容特征（如 PDF 的标题/作者、图片的 EXIF）自动判断类别 |
| **分类维度** | 文档、图片、视频、音乐、应用、压缩包、代码、设计源文件等 |
| **智能标签** | 自动提取时间标签（今天/本周/本月）、项目标签（基于文件夹关联） |
| **学习机制** | 用户手动移动文件后，系统学习偏好，下次自动优化 |
| **一键整理** | 点击按钮，桌面所有未分类文件自动归入对应智能分区 |
| **存储分布** | 可视化堆叠条展示各类文件占比，一目了然 |

**技术方案**: 本地规则引擎（扩展名白名单）+ 可选云端轻量模型（离线优先）

**设计页面**: `file-classify.html`、`home.html`

---

#### 2. 一键整理与自定义分区 ✨📁

| 特性 | 说明 |
|------|------|
| **智能分区** | 系统自动创建"文档""图片""视频"等默认分区 |
| **自定义分区** | 用户可拖拽创建任意分区，自定义名称、颜色、图标 |
| **分区规则** | 支持按扩展名、文件名正则、修改时间等设置自动入区规则 |
| **分区样式** | 圆角卡片 / 毛玻璃面板 / 极简线条 三种风格可选 |
| **磁吸对齐** | 分区靠近时自动吸附对齐，支持网格对齐模式 |

---

#### 3. 文件夹映射 / 磁盘映射 🗂️（Q-Dir 风格）

| 特性 | 说明 |
|------|------|
| **多窗格布局** | 支持 1×4（横向四栏）、2×2（田字格）、3×1（纵向三栏）等布局 |
| **任意映射** | 本地文件夹、磁盘根目录、网络共享（SMB/NAS）、FTP、云盘（OneDrive/阿里云盘） |
| **跨窗格操作** | 支持拖拽跨窗格复制/移动文件，右键快捷操作 |
| **快速导航** | 面包屑路径栏 + 书签收藏 + 历史记录 |
| **预览面板** | 选中文件右侧显示缩略图/基本信息/操作按钮 |
| **磁盘用量** | 每个窗格底部显示容量进度条（颜色预警 + 80% 阈值标记） |
| **大文件扫描** | 一键扫描磁盘大文件，快速释放空间 |
| **文件类型分布** | 每个磁盘底部迷你堆叠条，展示文档/图片/视频/其他占比 |

**参考**: Q-Dir、Total Commander、Directory Opus

**设计页面**: `disk-mapper.html`

---

#### 4. 桌面美化 🎨

| 特性 | 说明 |
|------|------|
| **壁纸引擎** | 支持静态图 / 动态视频壁纸 / 网页壁纸（类似 Wallpaper Engine 轻量版） |
| **主题系统** | 深色 / 浅色 / 自动跟随系统；支持自定义强调色 |
| **动态效果** | 粒子连线背景、鼠标光晕拖尾、窗口打开/关闭动效 |
| **毛玻璃面板** | 桌面组件和分区支持 backdrop-blur 效果，透明度可调 |
| **深度主题定制** | 全局字体、颜色、圆角、透明度、毛玻璃/亚克力效果均可自定义 |

**设计页面**: `wallpaper-square.html`、`wallpaper-detail.html`、`settings.html`

---

#### 5. 图标管理 🎨

| 特性 | 说明 |
|------|------|
| **图标着色** | 自动将桌面图标统一为某一种色调，与壁纸更协调；支持"自动匹配壁纸"提取主色 |
| **图标替换** | 允许用户批量或单独更换软件、文件夹的图标 |
| **图标包库** | 内置 Fluent / Papirus / Numix / Tela / WhiteSur 等图标包，一键切换 |
| **着色范围** | 全部图标 / 仅文件夹 / 仅应用 / 仅系统，可细分控制 |
| **着色强度** | 滑块调节着色强度，支持保留原始系统图标色彩 |
| **Before/After 预览** | 着色前后的对比预览，所见即所得 |
| **导入自定义图标包** | 支持导入用户自有图标包，导出/导入配色配置 |

**设计页面**: `icon-manager.html`

---

#### 6. 桌面组件 📋

| 组件 | 功能描述 |
|------|----------|
| **🕐 时钟** | 数字/模拟双模式，多时区支持，番茄钟集成，渐变弧形装饰 |
| **📅 日历** | 月视图/周视图/日视图，支持添加日程，与系统日历同步 |
| **📋 待办清单** | 快速添加任务，支持优先级标记、截止日期、子任务 |
| **⏰ 定时提醒** | 单次/循环提醒，支持倒计时、整点报时、自定义铃声，圆环进度 |
| **🌤️ 天气** | 当前天气 + 未来3天预报，自动定位 |
| **📊 系统监控** | CPU/内存/磁盘/网络实时监控小部件 |
| **📝 便签** | 桌面便利贴，支持颜色标记、置顶 |

**组件特性**: 自由拖拽位置、大小可调、支持吸附网格、可折叠为图标、S/M/L 三种尺寸

**组件商店**: 倒计时、股票行情（Pro）、快捷启动、番茄钟等扩展组件

**设计页面**: `desktop-widgets.html`、`desktop-preview.html`

---

#### 7. 任务栏增强 🚀

| 特性 | 说明 |
|------|------|
| **应用分组** | 拖拽应用图标创建分组（工作/娱乐/工具/开发等），组名和颜色自定义 |
| **分组折叠** | 点击分组名可展开/折叠组内应用，节省空间 |
| **隐藏区** | 不常用应用移入隐藏区，点击展开，支持快捷键呼出 |
| **最近文档** | 右键应用图标显示最近打开的文档列表 |
| **进度指示** | 下载/复制等操作在任务栏图标上显示环形进度 |
| **视觉增强** | 毛玻璃背景、动态图标（如天气应用显示实时天气图标）、圆角大小可调 |
| **位置支持** | 底部（默认）/ 顶部 / 左侧 / 右侧 |
| **Dock 栏** | 可选仿 macOS Dock 栏模式 |
| **托盘收纳** | 不常用窗口最小化到系统托盘，释放任务栏空间（Traymond 式） |

**设计页面**: `taskbar.html`

---

### B. 效率增强

#### 8. 快速启动器 🚀

| 特性 | 说明 |
|------|------|
| **全局呼出** | 自定义快捷键（默认 `Alt+Space`）随时呼出启动器 |
| **即时计算** | 输入算式直接显示结果（如 `12 * 8 + 45 = 141`），可复制 |
| **应用搜索** | 快速搜索并打开已安装应用 |
| **Everything 集成** | 与 Everything 秒级文件搜索引擎结合，毫秒级全盘搜索 |
| **命令执行** | 直接执行计算器、翻译等命令，支持中英互译预览 |
| **文件搜索** | 显示完整文件路径，支持拼音首字母模糊匹配 |
| **网页搜索** | 一键跳转 Google 搜索，`Ctrl+Enter` 快捷触发 |
| **悬浮球** | 常驻桌面的悬浮球作为入口，点击呼出常用功能菜单 |
| **分类切换** | `Tab` 键在应用/文件/命令/网页之间切换 |

**设计页面**: `quick-launch.html`

---

#### 9. 窗口管理增强 🪟

| 特性 | 说明 |
|------|------|
| **预设布局** | 左右分屏、三分屏、四宫格、主从布局、层叠、自定义，一键排列 |
| **窗口置顶** | 让特定窗口始终显示在最前 |
| **透明度调节** | 可调节任意窗口透明度，方便对照参考 |
| **最小化到托盘** | 不常用窗口最小化到系统托盘，释放任务栏空间 |
| **窗口规则** | 自动化规则：打开某应用时自动半屏/置顶；窗口超过阈值提示整理 |
| **Snap 吸附** | 拖动窗口到屏幕边缘自动吸附，5 个吸附区域可视化预览 |
| **活动窗口列表** | 集中查看所有活动窗口，快速切换/置顶/调透明度/最小化到托盘 |

**设计页面**: `window-manager.html`

---

#### 10. 自动化任务 ⚡

| 特性 | 说明 |
|------|------|
| **文件自动处理** | 为特定文件夹设置规则（如"超过30天的文件自动移入回收站"），类似 macOS Hazel |
| **条件触发** | 监控文件夹、文件类型、文件名匹配、修改时间、文件大小等多维条件 |
| **执行动作** | 重命名、移动、复制、删除（回收站）、备份到 NAS、增量 Git 同步 |
| **应用自动化** | 模拟鼠标键盘操作，实现应用登录等简单任务的自动化 |
| **执行日志** | 记录每次规则执行的详情，成功/跳过/失败状态可追溯 |
| **规则模板** | 内置推荐模板：同步设计稿到云端、清理空文件夹、批量重命名、按类型整理桌面 |
| **变量支持** | 文件名支持 `{日期}`、`{时间}` 等变量模板 |

**设计页面**: `automation-rules.html`

---

### C. 交互体验

#### 11. 创新交互方式

| 特性 | 说明 |
|------|------|
| **抽屉式/边缘触发** | 将常用文件收纳在屏幕边缘的"抽屉"中，鼠标移至边缘呼出，用完自动隐藏 |
| **悬浮球** | 常驻桌面的悬浮球作为入口，点击呼出常用功能或快捷菜单 |
| **鼠标手势** | 通过特定的鼠标轨迹执行返回、刷新、关闭窗口等操作 |
| **双击隐藏** | 双击桌面空白处，一键隐藏/显示所有桌面图标 |
| **专注模式** | 高亮当前活动窗口，自动淡化或隐藏其他所有内容，减少视觉干扰 |
| **多桌面/虚拟桌面** | 创建多个独立虚拟桌面，为工作/娱乐等场景设置独立的图标布局和壁纸 |

---

### D. 系统深度整合

#### 12. 控制中心 ⚙️

| 特性 | 说明 |
|------|------|
| **系统状态概览** | CPU / 内存 / GPU / 网络实时监控，一目了然 |
| **快捷开关** | WiFi、蓝牙、夜间模式、飞行模式、勿扰模式、专注模式、省电模式、性能模式 |
| **系统工具入口** | 一键访问控制面板、任务管理器、注册表编辑器、设备管理器、服务管理、事件查看器、CMD、PowerShell、磁盘管理 |
| **右键菜单增强** | 文件右键增加"复制路径"（`Ctrl+Shift+C`）、"在此处打开终端"、"快速压缩/解压"（7-Zip 集成） |
| **QuickLook 预览** | 选中文件后按下空格键，快速预览内容，无需打开软件 |

**设计页面**: `control-center.html`

---

#### 13. 全局搜索 🔍

| 特性 | 说明 |
|------|------|
| **触发方式** | 快捷键（默认 `Ctrl + Space`）/ 任务栏搜索按钮 / 桌面悬浮搜索框 |
| **搜索范围** | 本地文件、应用、设置项、浏览器书签、历史记录、网页搜索 |
| **Everything 引擎集成** | 底层文件搜索接入 Everything SDK，毫秒级全盘搜索，索引 28 万+ 文件秒级响应；未安装 Everything 时自动降级为内置索引 |
| **实时索引** | Everything 引擎自动维护全盘索引，零额外开销；内置索引作为降级方案，后台增量更新 |
| **模糊匹配** | 支持 Everything 语法（如 `*.docx`、`d:\项目`）+ 拼音首字母搜索（如输入"sj"匹配"设计"） |
| **关键词高亮** | 搜索结果中匹配文本高亮显示 |
| **预览面板** | 选中结果右侧显示文件预览/应用信息/快捷操作 |
| **分类筛选** | 搜索结果按文件/应用/设置/网页分类展示，带结果计数 |
| **快捷操作** | `Enter` 打开、`Ctrl+Enter` 在文件夹中显示、`Shift+Enter` 以管理员运行 |
| **最近搜索** | 记录搜索历史，快速重复搜索 |
| **键盘导航** | `↑↓` 导航、`Enter` 打开、`Esc` 关闭 |
| **引擎状态** | 搜索框底部实时显示 Everything 连接状态和索引文件数 |

**设计页面**: `global-search.html`

---

## 三、设计系统

### 视觉规范

| 令牌 | 值 | 用途 |
|------|------|------|
| `--dt-background` | `#0f1117` | 全局背景 |
| `--dt-foreground` | `#e8eaed` | 主文字 |
| `--dt-card` | `#1a1d27` | 卡片背景 |
| `--dt-primary` | `#6c5ce7` | 品牌色/强调色 |
| `--dt-muted` | `#2a2d3a` | 次级背景 |
| `--dt-muted-foreground` | `#9ca3af` | 次级文字 |
| `--dt-border` | `#2e3142` | 边框 |
| `--dt-state-success` | `#10b981` | 成功/正常 |
| `--dt-state-warning` | `#f59e0b` | 警告/注意 |
| `--dt-state-error` | `#ef4444` | 错误/危险 |
| `--dt-state-info` | `#3b82f6` | 信息/链接 |

### 字体

`"Inter", "PingFang SC", "Microsoft YaHei", system-ui, -apple-system, sans-serif`

### 圆角

- 小: `6px`（按钮、标签）
- 中: `10px`（卡片、输入框）
- 大: `16px`（面板、弹窗）

### 交互模式

- 深色主题为主，浅色/自动跟随系统为备选
- 卡片悬浮: `hover:border-primary/30` + `shadow` 增强
- 按钮悬浮: 微上移 `hover:-translate-y-0.5` + 阴影
- 图标: Lucide 图标库，语义化着色

---

## 四、技术架构

> **v2.1 重大修正**: 基于元气桌面竞品分析，桌面接管技术路线从 WorkerW 嵌入改为**独立透明窗口 + 精确 Z 序控制**。详见下方"竞品技术分析"与"桌面接管方案"。

### 竞品技术分析：元气桌面为什么做得好

元气桌面（原猎豹轻桌面，珠海豹趣科技）1000 万+ 活跃用户、50 万+ 壁纸资源，是目前 Windows 桌面美化领域最成功的商业产品。其核心优势在三个层面：

**技术层面 — 自研渲染引擎 + 性能优先**

- 自研动态壁纸引擎，利用图像算法 + 粒子特效让静态壁纸动起来，类似游戏画面渲染
- 深度整合 GPU 硬件加速（DirectX/OpenGL），复杂 3D 粒子、流体模拟、高清视频高效呈现
- H.265/HEVC 编码，高品质动态壁纸文件体积大幅压缩，内存占用低
- **智能后台管理（关键）**：检测到全屏应用（游戏/视频）时自动进入"省电模式"，暂停动态壁纸渲染，资源占用降到最低
- 智能适配引擎：根据硬件规格、分辨率、系统版本自动调整渲染策略，低配电脑也能流畅运行
- 低功耗模式，笔记本电池续航优先

**产品层面 — 三合一 + 零门槛**

- 壁纸 + 整理 + 组件三合一，不需要分别装 Wallpaper Engine + Fences + 桌面日历
- 桌面助手一键启用，按文件类型/使用频率自动分类，勾选"自动整理"即可
- 自定义分组与标签，按项目/任务/类型创建分组
- 桌面小组件（日历、时钟、天气、待办）自由摆放
- "天气联动"和"音乐律动"壁纸 — 独创交互壁纸，桌面实时反映天气或随音乐节奏舞动

**内容层面 — 海量 + 社区**

- 50 万+ 壁纸库，每日更新，覆盖风景/动漫/科幻/游戏等
- 用户共创计划，邀请设计师投稿原创壁纸
- 分类标签 + 搜索优化

**元气桌面的桌面接管方案（关键启发）**

元气桌面**不走 WorkerW 嵌入路线**，而是使用独立透明窗口 + 精确 Z 序控制：

1. 独立透明窗口覆盖桌面工作区（非全屏），通过 `SetWindowPos` 控制 Z 序
2. 窗口 Z 序位于桌面图标层（SHELLDLL_DefView）上方、任务栏下方
3. `WS_EX_LAYERED` 实现透明，桌面区域 `WS_EX_TRANSPARENT` 透传鼠标点击
4. 组件区域通过 hit-test 返回 `HTCLIENT` 捕获鼠标事件
5. 壁纸通过单独渲染窗口替换，不依赖 WorkerW
6. 退出时恢复默认壁纸 + 销毁透明窗口

这解释了之前 Tauri 方案 4 次尝试 WorkerW 全部失败的根本原因——**方向错了**。

### 架构概览

```
┌──────────────────────────────────────────────────────────────┐
│                        UI Layer (WPF .NET 8)                   │
│                                                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │ 桌面接管窗口  │  │  壁纸渲染窗口  │  │    主设置窗口         │ │
│  │ (透明覆盖层)  │  │ (GPU 加速)   │  │  (标准 WPF 窗口)     │ │
│  │ 组件/分区/搜索 │  │ 动态/静态壁纸 │  │  所有功能面板入口     │ │
│  └──────────────┘  └──────────────┘  └──────────────────────┘ │
│                                                                │
│  Z 序: 壁纸层 < 桌面图标层(SHELLDLL_DefView) < 接管窗口 < 任务栏 │
├──────────────────────────────────────────────────────────────┤
│                    Core Service Layer (.NET 8)                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 文件监控  │ │ 分类引擎  │ │ 索引服务  │ │ 提醒调度  │        │
│  │(FileWatch)│ │(Classifier)│ │(Everything)│ │(Scheduler)│       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 窗口管理  │ │ 自动化    │ │ 性能监控  │ │ 热键调度  │        │
│  │(WinMgr)  │ │(Automation)│ │(PerfMon) │ │(Hotkey)  │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
├──────────────────────────────────────────────────────────────┤
│                    Data & Storage Layer                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │ SQLite DB │ │ 配置文件  │ │ 缓存目录  │                    │
│  │ (元数据)   │ │ (JSON)   │ │ (缩略图)  │                    │
│  └──────────┘ └──────────┘ └──────────┘                    │
├──────────────────────────────────────────────────────────────┤
│                    Native Interop Layer                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ Win32 API │ │ DXGI/D2D │ │ Everything│ │ Shell API │       │
│  │(窗口/钩子) │ │(壁纸渲染) │ │  SDK     │ │(图标/文件) │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
└──────────────────────────────────────────────────────────────┘
```

### 技术选型（v2.1 确定）

| 模块 | 选定方案 | 备选 | 理由 |
|------|----------|------|------|
| **UI 框架** | **WPF (.NET 8)** | WinUI 3 | 原生 Windows 集成最佳，Win32 互操作成熟，透明窗口/分层窗口支持完善 |
| **桌面接管** | **独立透明窗口 + Z 序控制** | ~~WorkerW 嵌入~~ | 元气桌面验证路线，可靠不破坏原生交互；WorkerW 方案已证伪 |
| **壁纸引擎** | **DirectX 11 + DirectComposition** | MPV 嵌入 | GPU 硬件加速，支持粒子/流体/视频，H.265 解码 |
| **文件监控** | **FileSystemWatcher** | ReadDirectoryChangesW | .NET 原生封装，事件驱动 |
| **全文索引** | **Everything SDK 集成** | SQLite FTS5 | 毫秒级全盘搜索，零额外索引开销；未安装时降级为内置 FTS5 |
| **窗口管理** | **SetWindowPos + DwmSetWindowAttribute** | — | 置顶/透明度/Snap 均通过 Win32 API 实现 |
| **全局热键** | **RegisterHotKey** | 低级键盘钩子 | 官方 API，不拦截按键，安全软件友好 |
| **打包分发** | **MSIX** | Inno Setup | 沙箱隔离、自动更新、商店上架 |
| **代码签名** | **EV 代码签名证书** | — | 避免杀毒软件误报 |

### 桌面接管方案（v2.1 确定）

**放弃 WorkerW，改用独立透明窗口 + 精确 Z 序控制**

之前 Tauri + Rust 方案的失败教训：
1. `fullscreen:true` + 隐藏 SHELLDLL_DefView → 覆盖任务栏，不可点击
2. 壁纸层 WorkerW 嵌入 → 窗口被壁纸遮挡，不可见
3. `alwaysOnTop:true` 透明覆盖层 → 组件浮于所有窗口之上，桌面不可点击
4. 工作区大小 + 不 alwaysOnTop → "找不到壁纸层 WorkerW"

**失败根因**: WorkerW 壁纸层嵌入方案本身不可靠。Windows 的窗口层级在不同版本/更新中行为不一致，且嵌入后窗口被壁纸层完全遮挡。元气桌面等成熟产品均不走此路线。

**新方案 — 独立透明窗口 + Z 序控制**:

```
窗口层级 (Z 序，从底到顶):
┌─────────────────────────────────┐
│  ① 壁纸渲染窗口 (独立窗口)       │  ← 替换原生壁纸，GPU 加速渲染
├─────────────────────────────────┤
│  ② 桌面图标层 (SHELLDLL_DefView) │  ← 原生桌面图标，保持不变
├─────────────────────────────────┤
│  ③ 桌面接管窗口 (透明分层窗口)   │  ← zDesktop 核心：组件/分区/搜索
├─────────────────────────────────┤
│  ④ 任务栏 (Shell_TrayWnd)       │  ← 原生任务栏，保持不变
└─────────────────────────────────┘
```

**实现步骤**:

1. **壁纸渲染窗口**: 创建独立全屏窗口，Z 序设为最底层（`HWND_BOTTOM`），使用 DirectX 11 + DirectComposition 渲染动态/静态壁纸
2. **桌面接管窗口**: 创建无边框透明窗口，覆盖桌面工作区（`SystemParametersInfo(SPI_GETWORKAREA)` 获取大小，排除任务栏）
   - `WS_EX_LAYERED` — 启用分层窗口，支持 per-pixel alpha
   - `WS_EX_TRANSPARENT` — 默认透传所有鼠标点击到下层桌面图标
   - 通过 `SetWindowPos(hwnd, hwndDefView, ...)` 将接管窗口的 `hwndInsertAfter` 设为桌面图标层窗口（`SHELLDLL_DefView` 的 HWND），精确插入其上方但**不置顶**（避免使用 `HWND_TOPMOST`，否则会浮于所有窗口之上）
3. **区域级交互**: 组件/分区区域通过 `SetWindowRgn` 或 hit-test (`WM_NCHITTEST` 返回 `HTCLIENT`) 选择性捕获鼠标，其余区域透传
4. **Z 序维护**: 监听 `WM_WINDOWPOSCHANGING`，当其他窗口试图插入到接管窗口和桌面之间时，重新调整 Z 序
5. **全屏检测**: 监听前台窗口变化，检测到全屏应用时暂停动态壁纸渲染 + 隐藏接管窗口（元气桌面式智能后台管理）
6. **退出恢复**: 销毁透明窗口 + 壁纸渲染窗口，恢复默认壁纸

**关键技术点**:

| 技术点 | API | 说明 |
|--------|-----|------|
| 透明分层窗口 | `CreateWindowEx(WS_EX_LAYERED)` | 支持 per-pixel alpha 混合 |
| Z 序控制 | `SetWindowPos(hwnd, hwndInsertAfter, ...)` | 精确插入到桌面图标层上方 |
| 鼠标透传 | `WS_EX_TRANSPARENT` + hit-test | 桌面区域透传，组件区域捕获 |
| 工作区获取 | `SystemParametersInfo(SPI_GETWORKAREA)` | 排除任务栏的工作区域 |
| 全屏检测 | `SHQueryUserNotificationState` | 检测全屏应用/游戏模式 |
| 壁纸渲染 | DirectX 11 + DirectComposition | GPU 加速，低资源占用 |
| 性能监控 | `QueryPerformanceCounter` + ETW | 实时监控 CPU/GPU/内存占用 |

**性能保障策略（借鉴元气桌面）**:

- 智能后台管理：全屏应用时自动暂停动态效果 + 隐藏接管窗口
- 性能分级：高/中/低三档自动切换，根据硬件配置调整渲染参数
- 资源动态调度：仅在需要时加载壁纸资源，多线程异步加载
- 低功耗模式：笔记本电池模式下降帧率、关闭粒子效果
- 资源占用目标：内存 < 150MB，CPU 空闲 < 1%，全屏时 < 0.1%

---

## 五、交互设计

> **v2.3 新增**: 建立三层交互体系——全局导航串联、页面间跳转流程、页面内功能交互，确保 16 个设计页面形成可走通的完整体验闭环。

### 5.1 设计原则

1. **不打扰**: 默认静默运行，仅在需要时弹出通知
2. **原生感**: 视觉风格与 Windows 11 设计语言一致，不突兀
3. **零学习**: 操作逻辑符合直觉，右键菜单、拖拽等习惯保留
4. **可回退**: 一键整理前自动创建还原点，支持撤销
5. **高性能**: 内存占用 < 150MB，CPU 空闲时 < 1%
6. **不破坏原生**: 桌面图标可点击、任务栏可用、窗口切换正常、退出后桌面恢复

### 5.2 全局导航体系

所有页面共享左侧固定侧边栏，分组与页面归属如下：

| 分组 | 页面 | 导航图标 | 页面 ID |
|------|------|----------|---------|
| — | 首页概览 | `layout-dashboard` | page-home |
| 美化 | 壁纸广场 | `layout-grid` | page-wallpaper-square |
| 美化 | 壁纸详情（子页，不显示在侧边栏） | — | page-wallpaper-detail |
| 美化 | 桌面组件 | `widgets` | page-desktop-widgets |
| 美化 | 任务栏 | `panel-bottom` | page-taskbar |
| 美化 | 图标管理 | `palette` | page-icon-manager |
| 管理 | 文件分类 | `folder-tree` | page-file-classify |
| 管理 | 磁盘映射 | `hard-drive` | page-disk-mapper |
| 效率 | 全局搜索 | `search` | page-global-search |
| 效率 | 快速启动器 | `rocket` | page-quick-launch |
| 效率 | 窗口管理 | `app-window` | page-window-manager |
| 效率 | 自动化规则 | `zap` | page-automation-rules |
| 系统 | 控制中心 | `settings-2` | page-control-center |
| 系统 | 设置 | `settings` | page-settings |
| — | 桌面效果图（独立预览页） | `monitor` | page-desktop-preview |
| — | ⏸ 剪贴板历史（暂缓） | `clipboard` | page-clipboard-manager |

**侧边栏交互规则**:
- 当前页对应的导航项高亮（`bg-primary/10 text-primary`）
- 点击任意导航项跳转到对应页面
- 点击侧边栏顶部 logo / 「首页」返回首页概览
- 壁纸详情为壁纸广场的子页，不独立出现在侧边栏，通过返回按钮回到壁纸广场

**全局热键**（跨页面触发，任意时刻可用）:

| 热键 | 功能 | 说明 |
|------|------|------|
| `Ctrl+Space` | 呼出全局搜索 | 无论当前在哪个页面，直接打开全局搜索页 |
| `Alt+Space` | 呼出快速启动器 | 悬浮式启动器覆盖当前页面 |
| `Win+D` | 显示/隐藏桌面图标 | 等效于双击桌面空白处 |
| `Ctrl+Shift+C` | 复制文件路径 | 文件右键菜单增强项 |
| `Esc` | 关闭当前浮层/搜索 | 全局通用关闭行为 |

### 5.3 页面间跳转流程

首页概览作为枢纽页（hub），向外辐射到各功能页：

| 来源页 | 触发元素 | 目标页 | 说明 |
|--------|---------|--------|------|
| 任意页 | 侧边栏各导航项 | 对应功能页 | 全局导航 |
| 任意页 | 侧边栏 logo | 首页概览 | 快速回首页 |
| 首页概览 | 「一键整理」按钮 | 文件分类 | 直接触发整理流程 |
| 首页概览 | 「全局搜索」按钮 | 全局搜索 | — |
| 首页概览 | 「更换壁纸」按钮 | 壁纸广场 | — |
| 首页概览 | 「添加组件」按钮 | 桌面组件 | — |
| 首页概览 | 最近文件列表项 | 文件分类（预览态） | 定位到该文件 |
| 首页概览 | 今日待办项 | 桌面组件 | 聚焦待办组件 |
| 壁纸广场 | 壁纸卡片 | 壁纸详情 | 查看大图与详情 |
| 壁纸详情 | 返回按钮 | 壁纸广场 | 回到列表 |
| 壁纸详情 | 「设为桌面」按钮 | 桌面效果图 | 预览壁纸效果 |
| 文件分类 | 分类卡片 | 磁盘映射 | 查看文件存储位置 |
| 文件分类 | 「整理到分区」 | 磁盘映射 | 跳转到分区管理 |
| 桌面组件 | 组件商店「添加」 | 桌面效果图 | 预览组件效果 |
| 桌面效果图 | 任务栏搜索图标 | 全局搜索 | 模拟真实任务栏入口 |
| 桌面效果图 | 任务栏设置图标 | 设置 | — |
| 桌面效果图 | 浮动搜索框 | 全局搜索 | — |
| 全局搜索 | 搜索结果项 | 文件分类（预览态） | 打开文件定位 |
| 控制中心 | 系统工具入口 | 设置 | 跳转到对应设置项 |
| 设置 | 壁纸设置区 | 壁纸广场 | 快速换壁纸 |
| 设置 | 组件管理区 | 桌面组件 | 快速管理组件 |

### 5.4 页面内功能交互

每个页面内部的控件交互行为定义：

| 页面 | 控件 | 交互行为 | 状态变化 |
|------|------|----------|----------|
| **文件分类** | 一键整理按钮 | 点击 → 进度动画 → 完成态 | 按钮变完成图标 + 显示整理数量 |
| 文件分类 | 分类开关 | toggle | 开/关切换，分类区域淡入/淡出 |
| 文件分类 | 存储分布图 | hover 各类块 | 高亮 + tooltip 显示数量 |
| **磁盘映射** | 窗格路径栏 | 点击面包屑 | 切换文件夹路径 |
| 磁盘映射 | 跨窗格拖拽 | 拖拽文件 | 复制/移动文件到目标窗格 |
| 磁盘映射 | 大文件扫描按钮 | 点击 | 扫描进度 → 结果列表 |
| **桌面组件** | 组件卡片 | 拖拽 | 自由移动位置 + 网格吸附 |
| 桌面组件 | 尺寸切换 S/M/L | 点击切换 | 卡片尺寸平滑过渡 |
| 桌面组件 | 组件商店添加按钮 | 点击 | 弹出商店浮层 → 选择 → 添加到桌面 |
| 桌面组件 | 折叠为图标 | 点击折叠按钮 | 卡片收起为图标，再点击展开 |
| **全局搜索** | 搜索输入框 | 输入 | 实时搜索结果 + 关键词高亮 |
| 全局搜索 | `↑↓` 键盘导航 | 按键 | 上下选中结果项 + 右侧预览联动 |
| 全局搜索 | `Tab` 分类切换 | 按键 | 切换 文件/应用/设置/网页 分类 |
| 全局搜索 | `Enter` 打开 | 按键 | 打开选中项 |
| 全局搜索 | Everything 引擎状态 | 实时 | 绿色脉冲 = 已连接，灰色 = 降级模式 |
| **快速启动器** | 命令输入框 | 输入算式 | 实时计算结果预览 |
| 快速启动器 | `Tab` 分类切换 | 按键 | 应用/文件/命令/网页 切换 |
| 快速启动器 | `Ctrl+Enter` | 按键 | 网页搜索跳转 |
| **窗口管理** | 布局预设卡片 | 点击 | 一键应用分屏布局到当前窗口 |
| 窗口管理 | 窗口列表项 | 点击置顶 | 窗口置顶状态切换 |
| 窗口管理 | 透明度滑块 | 拖拽 | 实时调节窗口透明度 |
| 窗口管理 | 最小化到托盘 | 点击 | 窗口移入托盘 |
| **任务栏** | 分组名 | 点击 | 折叠/展开组内应用 |
| 任务栏 | 应用图标拖拽 | 拖拽 | 重新排序 / 创建新分组 |
| 任务栏 | 隐藏区 | 点击展开 | 显示不常用应用 |
| **自动化规则** | 规则开关 | toggle | 启用/停用规则 |
| 自动化规则 | 新建规则按钮 | 点击 | 弹出规则编辑器浮层 |
| 自动化规则 | 规则模板卡片 | 点击 | 套用模板 → 进入编辑器 |
| 自动化规则 | 执行日志项 | 点击 | 展开执行详情 |
| **控制中心** | 快捷开关 | toggle | WiFi/蓝牙/夜间模式等 开/关 |
| 控制中心 | 系统工具入口 | 点击 | 打开对应系统工具 |
| 控制中心 | 性能监控图表 | hover | tooltip 显示详细数值 |
| **图标管理** | 着色强度滑块 | 拖拽 | 实时预览着色效果 |
| 图标管理 | 图标包切换 | 点击 | 一键切换图标包 + 预览 |
| 图标管理 | Before/After 预览 | 拖拽分割线 | 左右对比拖动 |
| **壁纸广场** | 分类筛选标签 | 点击 | 筛选壁纸列表 |
| 壁纸广场 | 壁纸卡片 | hover | 放大预览 + 收藏按钮浮现 |
| **壁纸详情** | 设为桌面按钮 | 点击 | 应用壁纸 → 跳转桌面效果图预览 |
| 壁纸详情 | 收藏按钮 | 点击 | 收藏/取消收藏 |
| **设置** | 主题切换 | 点击 | 深色/浅色/自动 实时预览 |
| 设置 | 透明度滑块 | 拖拽 | 毛玻璃透明度实时预览 |
| 设置 | 圆角滑块 | 拖拽 | 全局圆角实时预览 |
| 设置 | 开关项 | toggle | 各功能启用/停用 |

### 5.5 核心用户路径

以下 5 条路径覆盖产品最高频使用场景，每条路径均可从首页出发走通：

**路径 1 — 整理混乱桌面** 🧹
```
首页 → 点击「一键整理」→ 文件分类（进度动画→完成→显示整理数量）
    → 点击分类卡片 → 磁盘映射（查看各分区空间分布）
```

**路径 2 — 搜索并打开文件** 🔍
```
任意页 → Ctrl+Space → 全局搜索
    → 输入关键词（实时结果）→ ↑↓ 选中 → 右侧预览面板确认
    → Enter 打开
```

**路径 3 — 更换壁纸** 🎨
```
首页 → 点击「更换壁纸」→ 壁纸广场（浏览/筛选）
    → 点击壁纸卡片 → 壁纸详情（大图预览）
    → 点击「设为桌面」→ 桌面效果图（查看效果）
```

**路径 4 — 添加桌面组件** 📋
```
首页 → 点击「添加组件」→ 桌面组件（组件商店区）
    → 选择组件 → 点击「添加」→ 桌面效果图（查看组件效果）
    → 拖拽调整位置
```

**路径 5 — 创建自动化规则** ⚡
```
首页 → 侧边栏「自动化规则」→ 自动化规则页
    → 点击「新建规则」→ 规则编辑器浮层
    → 选择触发条件 → 设置执行动作 → 保存启用
```

### 5.6 交互状态视觉规范

| 状态 | 视觉表现 |
|------|----------|
| **默认** | 卡片 `bg-card border-border`；按钮次级态 `bg-card border` |
| **悬浮 hover** | 卡片 `border-primary/40` + `shadow-lg shadow-primary/10` + 微上移 `-translate-y-0.5`；`transition-all duration-300` |
| **选中/激活** | 导航项 `bg-primary/10 text-primary`；列表项左侧 `border-l-2 border-l-primary` |
| **按下 active** | 按钮微缩 `-translate-y-0` + `shadow-sm` |
| **禁用** | `opacity-50 cursor-not-allowed` |
| **加载中** | 进度条品牌色填充 + `pulse` 动画 |
| **成功** | `text-state-success` + check 图标 |
| **焦点 focus** | 输入框 `ring-2 ring-primary` + `shadow-lg shadow-primary/15` |

---

## 六、里程碑规划

| 阶段 | 周期 | 交付内容 |
|------|------|----------|
| **MVP** | 2周 | 桌面组件（时钟+日历+待办+天气+监控）+ 基础分区 + 桌面接管验证 |
| **v0.5** | 3周 | + 智能分类 + 一键整理 + 任务栏增强 + 图标管理 |
| **v0.8** | 2周 | + 多窗格映射 + 全局搜索 + 快速启动器 |
| **v1.0** | 2周 | + 窗口管理 + 自动化规则 + 控制中心 + 壁纸引擎 + polish |
| **v1.5** | 3周 | + 动态壁纸 + 鼠标手势 + 多虚拟桌面 + 专注模式 |
| **v2.0** | 持续 | 插件系统、云同步、AI 助手集成、Dock 栏 |

---

## 七、竞品分析

> **v2.1 新增**: 基于元气桌面深度调研，将其作为桌面接管与壁纸渲染的核心对标竞品，明确 zDesktop 的差异化路线。

### 核心对标：元气桌面

| 维度 | 元气桌面表现 | zDesktop 差异化策略 |
|------|------------|-------------------|
| **定位** | 壁纸 + 整理 + 组件三合一，以壁纸内容为核心 | 效率工具矩阵为核心，壁纸/美化为基础体验 |
| **用户规模** | 1000 万+ 活跃、50 万+ 壁纸 | 冷启动期，靠效率工具差异化突围 |
| **桌面接管** | 独立透明窗口 + Z 序控制（非 WorkerW） | 同路线借鉴，验证可行后直接复用 |
| **壁纸引擎** | 自研 GPU 加速、H.265、粒子/流体 | DirectX 11 + DirectComposition，轻量化优先 |
| **智能后台** | 全屏应用自动暂停渲染 | 复用此策略，增加性能分级与电池模式 |
| **效率工具** | 仅基础整理 + 日历/时钟/天气 | 启动器/窗口管理/自动化/Hazel 式规则 |
| **文件搜索** | 无全盘搜索 | Everything SDK 毫秒级搜索 |
| **隐私** | 云端壁纸下载、账号体系 | 本地优先、无强制账号、无广告 |
| **内容生态** | 50 万+ 壁纸库 + 社区共创 | MVP 阶段聚焦工具，壁纸生态后期建设 |

**核心结论**: 元气桌面证明了"独立透明窗口 + Z 序控制"路线可行，且市场验证了"壁纸+整理+组件"三合一需求。zDesktop 不在壁纸内容上正面竞争，而是以**效率工具矩阵 + 自动化 + 本地隐私**为差异化，切入元气桌面未覆盖的进阶用户需求。

### 全景竞品对比

| 产品 | 优势 | 劣势 | 我们的差异化 |
|------|------|------|-------------|
| **元气桌面** | 1000万用户、自研壁纸引擎、GPU加速、智能后台管理 | 效率工具弱、无全盘搜索、无自动化、云端依赖、广告 | +Everything搜索 +自动化规则 +窗口管理 +本地优先无广告 |
| **Fences** | 分区鼻祖，稳定 | 无智能分类、无组件、无搜索 | +AI分类 +组件 +搜索 +自动化 |
| **Q-Dir** | 多窗格文件管理强 | 无桌面管理、无美化 | 集成到完整桌面生态 |
| **Wallpaper Engine** | 壁纸效果极致 | 仅壁纸，无其他功能 | 轻量壁纸 + 完整桌面管理 |
| **BitDock** | 任务栏美化 | 仅限任务栏 | 全桌面覆盖 + Dock 可选 |
| **腾讯桌面整理** | 免费、易用 | 分类粗糙、广告、无隐私 | 本地优先、无广告、更智能 |
| **Listary/Everything** | 搜索极速 | 仅搜索，无桌面管理 | 搜索 + 桌面管理 + 启动器一体 |
| **Hazel (macOS)** | 自动化规则强 | 仅 macOS | Windows 首个 Hazel 式自动化 |
| **Alfred/Raycast** | 启动器生态丰富 | 仅 macOS | Windows 原生启动器 + 桌面管理 |

---

## 八、风险与应对

> **v2.1 修正**: 原"优先验证 WorkerW 方案"已证伪废弃，风险表全面更新为独立透明窗口 + Z 序控制路线的对应风险。

### 技术风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| Z 序被其他窗口抢占导致接管窗口错位 | 高 | 监听 `WM_WINDOWPOSCHANGING`，检测到插入时立即 `SetWindowPos` 重新校正；定时器周期性校验 Z 序 |
| 透明窗口与桌面图标点击区域冲突 | 高 | `WS_EX_TRANSPARENT` 全局透传 + 组件区域 hit-test 返回 `HTCLIENT` 精确捕获；提供"仅组件模式"降级 |
| 全屏应用/游戏时接管窗口造成卡顿或误触 | 高 | `SHQueryUserNotificationState` 检测全屏，立即隐藏接管窗口 + 暂停壁纸渲染（元气桌面式智能后台） |
| DirectX 壁纸渲染在高 DPI/多屏下错位 | 中 | `ID2D1Factory` + `DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2`，逐屏独立渲染上下文 |
| Everything 未安装时搜索降级体验差 | 中 | 自动检测安装状态，引导安装；降级方案用 SQLite FTS5 内置索引，功能完整但速度受限 |
| WPF 透明分层窗口性能瓶颈 | 中 | 组件区域用 `DComposition` 独立合成，避免整窗重绘；静态区域位图缓存 |

### 系统与兼容风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| Windows 更新导致 Z 序/窗口行为变化 | 高 | 核心接管逻辑抽象为独立模块，支持快速适配；每版本回归测试覆盖 Win10/11 |
| 杀毒软件误报透明窗口/全局热键 | 中 | EV 代码签名 + 提交白名单 + 开源核心接管模块 |
| 与第三方桌面工具（Fences/Wallpaper Engine）冲突 | 中 | 安装时检测冲突，提供兼容模式开关；检测到同类工具时仅启用效率模块 |
| 文件索引占用大量磁盘 | 中 | Everything 优先（零额外索引）；降级方案增量索引 + 可配置范围 + 压缩 |

### 产品风险

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| 与元气桌面定位重叠导致用户认知混淆 | 中 | 明确"效率优先 vs 壁纸优先"差异，首页突出启动器/自动化/窗口管理等元气桌面缺失功能 |
| 壁纸生态冷启动内容不足 | 中 | MVP 不主打壁纸，聚焦工具；支持导入本地壁纸 + Wallpaper Engine 工坊格式兼容 |
| 功能过多导致学习成本高 | 中 | 渐进式引导，核心功能默认开启，进阶功能折叠；提供"极简模式"一键隐藏全部美化 |

---

## 九、设计页面索引

共 16 个设计页面，已在 `.design` 画布注册（其中标注 ⏸ 的为暂缓功能，页面保留作为设计参考，不进入近期开发范围）：

| 页面 | 文件 | 模块归属 |
|------|------|----------|
| 桌面效果图 | `desktop-preview.html` | 全局预览 |
| 首页概览 | `home.html` | 仪表盘 |
| 壁纸广场 | `wallpaper-square.html` | 桌面美化 |
| 壁纸详情 | `wallpaper-detail.html` | 桌面美化 |
| 文件分类 | `file-classify.html` | 智能分类 |
| 桌面组件 | `desktop-widgets.html` | 桌面组件 |
| 任务栏 | `taskbar.html` | 任务栏增强 |
| 全局搜索 | `global-search.html` | 全局搜索 |
| 磁盘映射 | `disk-mapper.html` | 磁盘映射 |
| 设置中心 | `settings.html` | 系统设置 |
| ⏸ 剪贴板历史 | `clipboard-manager.html` | 效率增强（暂缓） |
| 快速启动器 | `quick-launch.html` | 效率增强 |
| 窗口管理 | `window-manager.html` | 效率增强 |
| 自动化规则 | `automation-rules.html` | 效率增强 |
| 控制中心 | `control-center.html` | 系统整合 |
| 图标管理 | `icon-manager.html` | 视觉个性化 |

---

## 十、后续规划（暂缓功能）

以下功能经评审暂不纳入近期开发范围，设计页面保留作为参考，待核心体验稳定后再行启动：

| 功能 | 原属模块 | 暂缓原因 | 重新评估时机 |
|------|----------|----------|-------------|
| **剪贴板历史管理** | 效率增强 | 优先聚焦桌面接管与搜索/启动器等高频核心路径；剪贴板涉及安全软件兼容与隐私策略，需单独设计权限模型 | v1.0 稳定后 |
| **音乐控制组件** | 桌面组件 | 桌面组件 MVP 以时钟/日历/待办/天气/监控为主，音乐控制需对接多播放器协议，投入产出比低 | v1.5 组件扩展期 |

---

*本设计案为 v2.2 版本：基于元气桌面竞品分析修正桌面接管技术路线（放弃 WorkerW，改用独立透明窗口 + Z 序控制），技术选型确定为 WPF .NET 8；暂缓剪贴板历史与音乐控制，移入后续规划。等待评审确认后进入开发实施阶段。*
