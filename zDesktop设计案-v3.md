# zDesktop 设计案 v3.0 — 不接管的桌面增强层

> **版本**: v3.0 | **日期**: 2026-07-25 | **状态**: 待评审
>
> **本版取代 v2.3**（`zDesktop设计案.md` 保留作为历史存档，不再作为开发依据）。
>
> **v3.0 为方向性重写，不是增量修订**。核心变更三条：
> 1. **放弃自渲染桌面图标层**——永不隐藏 `SHELLDLL_DefView`，原生桌面全程保持可用。
> 2. **功能从 13 模块砍到 3 条主线**，并给出显式的「不做清单」。
> 3. **「不破坏原生」从口号变成可自动化验证的硬门槛**，未过门槛不允许合入新功能。

---

## 零、为什么要重写

v2.3 是一份**功能清单**，不是产品定义。它列了 13 个模块、16 张设计稿、若干特性表格，但没有回答三个决定性问题：用户是谁、什么场景下打开它、为什么不用「Fences + Everything + PowerToys」这套免费组合。

代价已经在代码里兑现了。截至 v2.3 实现（约 1.3 万行 UI 代码、零测试）：

| v2.3 承诺 | 实现现状 |
|---|---|
| 核心原则「不破坏原生桌面」 | 默认隐藏原生图标层并自渲染，回收站/此电脑/副屏图标全部消失 |
| 壁纸引擎（DX11 + DirectComposition + H.265） | 仅 `SPI_SETDESKWALLPAPER` + 必应下载，零 GPU 代码 |
| 任务栏增强（整个模块 7） | 纯 UI mock，代码注释自述「不实际替换系统任务栏」 |
| 快速启动器（Alt+Space 悬浮） | Alt+Space 打开大设置窗口的搜索页；且与全局搜索热键绑反 |
| 高风险项对策（Z 序自愈 / 全屏检测 / 多屏 / DPI） | 四项全部未实现 |

根因是设计案把 90% 的交互笔墨给了「16 个页面之间怎么跳转」，于是实现把 90% 的代码给了那个 16 页的设置窗口。**但这个产品真正的界面是桌面本身、Alt+Space 和托盘，设置窗口只是配置面板。**

---

## 一、产品定位

**zDesktop 是 Windows 原生桌面的增强层——它在原生桌面之上叠加原生做不到的能力，而不替换原生的任何部分。**

一句话检验标准：**任何时刻杀掉 zDesktop，桌面必须与从未安装过完全一致。**

### 目标用户

桌面文件常年 50+ 个、习惯用桌面当工作台的 Windows 重度用户。他们已经知道 Fences 和 Everything 存在，但不想装四个软件、也不想为「一个画框工具」付 10 美元订阅。

### 为什么用户要换

| 对手 | 它的问题 | zDesktop 的答案 |
|---|---|---|
| Fences | 收费、只有分区、无自动化 | 分区 + 组件 + 规则一体，本地免费 |
| 元气桌面 | 广告、云端账号、效率工具弱 | 无广告、无账号、本地优先 |
| 腾讯桌面整理 | 分类粗糙、隐私存疑 | 规则可编辑、全本地 |
| PowerToys | 不管桌面 | 桌面是主场 |
| 什么都不装 | 桌面越用越乱 | 规则一次配置，长期自动维持 |

**差异化落点：Windows 上没有 Hazel。** 「文件落到桌面 → 按规则自动归入分区/移走」这条链路是本产品唯一没有成熟免费替代品的能力，也是长期护城河。

---

## 二、第一原则：零破坏契约

这一节的优先级高于本文档其余全部内容。**违反本节的功能一律不做，无论它多有吸引力。**

1. **永不隐藏、永不替换 `SHELLDLL_DefView` / `SysListView32`。** 桌面图标始终由 Explorer 渲染和管理。回收站、此电脑、多选框选、F2 重命名、与资源管理器互拖、实时刷新、右键菜单——全部是 Explorer 自带的，我们一行都不重写。
2. **永不使用 `HWND_TOPMOST`** 作为常驻状态。覆盖层通过 Z 序锚定在图标层之上、任务栏之下。
3. **所有对系统状态的修改必须可逆且有还原记录**：壁纸、图标位置、快捷方式图标，改前落盘原值，卸载/退出/崩溃三种路径都能还原。
4. **强杀即恢复**。不依赖 `OnExit` 跑完。任何需要「下次启动时修复」的设计都不合格。
5. **全屏应用期间零存在感**。检测到全屏/游戏模式，覆盖层隐藏、定时器停摆。

---

## 三、功能范围

### 3.1 三条主线（P0/P1）

#### 主线一：桌面分区（Fences 那一层）—— P0

在**原生图标**之上做分组容器，而不是重画图标。

| 能力 | 说明 |
|---|---|
| 分区容器 | 半透明圆角矩形，自定义名称/颜色/大小/位置 |
| 图标归属 | 拖动原生图标进入容器范围即归属该容器；容器移动时组内图标跟随 |
| 容器内排布 | 网格自动排列、按名称/类型/时间排序、超出时容器内滚动 |
| 折叠 | 双击标题栏折叠为标题条，再双击展开 |
| 自动入区规则 | 按扩展名 / 文件名正则 / 修改时间自动归入指定容器（与主线三共用规则引擎） |
| 一键整理 | 桌面所有未归属文件按规则入区，**执行前落盘快照，支持一键撤销** |

**关键技术路线**：通过跨进程操作 `SysListView32`（`LVM_GETITEMPOSITION` / `LVM_SETITEMPOSITION`，配合 `VirtualAllocEx` 在 explorer.exe 中读写）控制原生图标位置。这是 Fences 的路线，也是唯一能既保留原生交互、又控制图标排布的方式。

**分区背景的绘制**（需 spike 验证，两条候选）：
- **候选 A（首选）：合成进壁纸。** zDesktop 把「用户壁纸 + 分区半透明矩形」合成为一张图，设为桌面壁纸。零 Z 序问题、零渲染开销、退出还原原壁纸即可。拖动分区时用临时交互层跟手，松手后重新合成落盘。
- **候选 B：DefView 之下的独立背景窗口。** 视觉更灵活（可做实时模糊/动效），但 Z 序维护复杂度高。

M2 阶段先用一周 spike 把两条都验一遍再定。**在 spike 出结论前，本节技术方案不算已确定。**

#### 主线二：桌面组件 —— P0

覆盖层窗口承载浮动组件，位于图标层之上、非置顶、非组件区域鼠标透传。这部分现有实现方向正确，需要硬化（见 §5）。

MVP 组件：时钟、日历、待办、天气、系统监控。均已有实现。

组件特性：自由拖拽、网格吸附、S/M/L 尺寸、折叠为图标、每组件独立设置面板。

**不做组件商店、不做付费组件、不做插件系统**（v2.0 前）。

#### 主线三：自动化规则（Windows 版 Hazel）—— P1

产品的差异化核心。现有 `AutomationService`（`FileSystemWatcher` 实现）是全仓完成度最高的模块，直接演进。

| 能力 | 说明 |
|---|---|
| 监控范围 | 桌面 + 用户指定文件夹（下载、截图目录等） |
| 触发条件 | 扩展名、文件名正则、大小、创建/修改时间、存在时长 |
| 执行动作 | 移动、复制、重命名（支持 `{日期}` 变量）、归入分区、删除到回收站、运行命令 |
| 安全阀 | 默认不做破坏性动作；删除一律进回收站；每条规则首次执行前干跑预览 |
| 执行日志 | 成功/跳过/失败可追溯，支持单条撤销 |
| 内置模板 | 「截图超过 7 天移入归档」「安装包用完即删」「按类型整理桌面」 |

**应用自动化（模拟键鼠）从范围内移除**——它是另一个产品，且触发杀软误报。

### 3.2 配套（P1/P2）

- **Alt+Space 轻量启动器**（P1）：独立无边框小窗口，**绝不打开设置窗口**。应用/文件搜索（Everything 可用时走 Everything，否则走内置索引）、算式计算、网页搜索。全局搜索与快速启动器**合并为同一个东西**，不做两套。
- **边缘抽屉**（P2）：鼠标移至屏幕边缘呼出常用文件收纳区。
- **设置窗口**（P0，但降级）：从「产品主体」降为纯配置面板。页面数从 16 砍到 6：概览、分区、组件、规则、外观、关于。

### 3.3 显式不做清单

写进设计案是为了防止复发。以下功能**不在 v1.0 范围内，提案一律拒绝**：

| 不做 | 原因 |
|---|---|
| 任务栏替换 / Dock 栏 | 需系统级 hook，与「零破坏」冲突；打不过 BitDock |
| 动态壁纸引擎（DX11/视频/网页壁纸） | 无底洞，正面撞 Wallpaper Engine 和元气桌面 |
| 壁纸广场 / 壁纸内容生态 | 冷启动无内容，且不是我们的战场 |
| 图标着色 / 图标包 | 不再自渲染图标，着色失去载体（图标替换见 P2 备注） |
| 多窗格文件管理器（Q-Dir） | 与「桌面增强层」定位无关，是独立产品 |
| 窗口管理（分屏/置顶/透明度） | PowerToys FancyZones 免费且更好 |
| 鼠标手势 / 虚拟桌面 / 专注模式 | 锦上添花，稀释精力 |
| 剪贴板历史 | 隐私模型需单独设计，Win+V 已内置 |
| 控制中心（系统开关聚合） | Windows 11 已有，重复造轮子 |

> P2 备注：**图标替换**若要做，唯一合规路线是改写桌面 `.lnk` 文件的图标指向（真实修改用户文件），必须配套备份 + 一键还原，且默认关闭。不通过隐藏原生层实现。

---

## 四、技术架构

```
┌──────────────────────────────────────────────────────────────┐
│  Explorer（不属于 zDesktop，我们只读写它，不替换它）             │
│  桌面壁纸  <  SHELLDLL_DefView / SysListView32（原生图标）      │
└──────────────────────────────────────────────────────────────┘
                            ▲                    ▲
             图标位置读写（LVM_*）        分区背景合成进壁纸
                            │                    │
┌──────────────────────────────────────────────────────────────┐
│  zDesktop.Shell.Fences                                        │
│  NativeIconController（跨进程 SysListView32 读写）            │
│  FenceLayout（容器模型/归属计算）  FenceCompositor（背景合成）  │
├──────────────────────────────────────────────────────────────┤
│  zDesktop.Shell.Desktop — 覆盖层                              │
│  OverlayWindow（DefView 之上，非置顶，hit-test 透传）          │
│  ZOrderKeeper（WM_WINDOWPOSCHANGING 自愈）                    │
│  MonitorSet（多屏：每屏一个 overlay 实例）                     │
│  FullscreenGuard（SHQueryUserNotificationState）              │
│  SafetyNet（崩溃/强杀恢复）                                    │
├──────────────────────────────────────────────────────────────┤
│  功能服务（与桌面层解耦，可独立测试）                            │
│  Automation（规则引擎）  Widgets（组件宿主）                    │
│  Launcher（Alt+Space）   Search（Everything / 内置索引）        │
├──────────────────────────────────────────────────────────────┤
│  Data：SQLite（规则/日志/索引） + JSON（布局/设置） + 还原快照   │
└──────────────────────────────────────────────────────────────┘
```

### 技术选型

沿用 WPF (.NET 8)，但补三项 v2.3 缺失的基础设施：

| 项 | 决定 |
|---|---|
| DPI | 新增 `app.manifest` 显式声明 `PerMonitorV2`；所有 Win32 像素↔DIP 换算统一走 `DpiHelper`，禁止在业务代码里手写换算 |
| 多屏 | `EnumDisplayMonitors` 枚举，**每个显示器一个 overlay 实例**；监听 `WM_DISPLAYCHANGE` + `WM_DPICHANGED` 重建 |
| UI 写法 | 新增页面一律 XAML + 数据绑定；存量代码页禁止继续膨胀（见 §5） |
| 测试 | 新增 `zDesktop.Tests`（xUnit）+ `zDesktop.SystemTests`（门槛回归，见 §六） |

---

## 五、存量代码处置

| 模块 | 处置 | 理由 |
|---|---|---|
| `AutomationService`（34KB） | **保留并演进** | 全仓质量最高，是主线三的底座 |
| `WidgetHost` / `WidgetContainer` / 7 个 Widget | **保留并硬化** | 主线二，方向正确 |
| `DesktopOverlayWindow` | **保留但重写核心** | 需补 Z 序自愈、多屏、DPI、全屏检测 |
| `EverythingBridge` / `FileIndexService` | **保留** | 启动器底座 |
| `Theme` / `LayoutStore` / `TrayIconManager` / `WallpaperService` | **保留** | `WallpaperService` 升级为分区背景合成的底座 |
| `MainWindow` + 13 个 Page | **大幅裁剪** | 16 页 → 6 页；砍掉的页删除而非注释 |
| `DesktopIconLayer` / `DesktopIconItem` / `DesktopIconStore` | **删除** | 自渲染图标层，与零破坏契约直接冲突 |
| `IconManagerService`（36KB） | **删除** | 着色依附于自渲染图标层，载体已不存在 |
| `TaskbarPage`（25KB，纯 mock） | **删除** | 不做任务栏 |
| `DiskMapperService` + `DiskMapperPage`（65KB） | **移出主线**，归档到 `experiments/` | 独立产品，不属于桌面增强层 |
| `WindowManagerService` + Page（35KB） | **删除** | 见不做清单 |
| `ControlCenterService` + Page（68KB） | **删除** | 见不做清单 |
| `GlobalSearchPage` + `QuickLaunchPage`（70KB） | **合并重写**为轻量启动器窗口 | 两套变一套，且不再寄生在设置窗口里 |

净效果：删除约 25 万字符的 UI 代码，主线代码占比从约 20% 提升到 70% 以上。

---

## 六、验收门槛（P0，先于一切功能）

以下 6 条必须做成**自动化回归测试**，每次合入前跑通。取代 v2.3 那些没人测的「内存 < 150MB」。

| # | 门槛 | 验证方式 |
|---|---|---|
| G1 | 多屏（≥2）+ 混合 DPI（100% / 150%）下，组件与分区位置正确、不越界 | 虚拟显示器 + 截图断言 |
| G2 | `taskkill /F` 强杀后，桌面壁纸/图标位置/图标可见性 100% 还原 | 杀进程 → 校验注册表与图标坐标 |
| G3 | Explorer 重启（结束 explorer.exe）后，覆盖层自动重新附着、Z 序正确 | 自动化重启 explorer → 校验 HWND 关系 |
| G4 | 全屏应用运行期间，覆盖层不可见且 zDesktop CPU < 0.5% | 启动全屏窗口 → 采样 |
| G5 | 卸载后无残留：无隐藏图标层、壁纸已还原、注册表自启项已清 | 卸载脚本 + 环境快照 diff |
| G6 | 显示器热插拔 / 分辨率切换后组件不丢失、不重叠 | 模拟 `WM_DISPLAYCHANGE` |

**规则**：G1–G6 未全绿，不允许开发任何新功能。

---

## 七、里程碑

| 阶段 | 周期 | 交付 | 出口标准 |
|---|---|---|---|
| **M0 止损** | 3 天 | 修 DPI 换算 bug、热键绑反、默认改回原生图标模式、删除 mock 页、建测试项目骨架 | 编译通过 + 默认启动不破坏桌面 |
| **M1 零破坏地基** | 2 周 | Z 序自愈、多屏 overlay、`PerMonitorV2`、全屏检测、崩溃/强杀恢复 | **G1–G6 全绿** |
| **M2 分区 spike** | 1 周 | 验证 `SysListView32` 跨进程控制 + 背景合成两条候选 | 出技术结论文档，定 A 或 B |
| **M3 分区落地** | 3 周 | 容器 CRUD、图标归属、排序、折叠、一键整理 + 撤销 | 分区可用且 G1–G6 保持绿 |
| **M4 组件打磨** | 2 周 | 5 个 MVP 组件、设置面板、布局持久化 | 组件在多屏/DPI 下正确 |
| **M5 自动化规则** | 2 周 | 规则编辑器、干跑预览、执行日志、撤销、3 个内置模板 | 规则误伤率为 0（干跑测试集） |
| **M6 启动器** | 2 周 | Alt+Space 轻量窗口、Everything 集成 + 降级、算式 | 冷启动呼出 < 150ms |
| **v1.0** | 2 周 | 安装包（Inno Setup）、代码签名、卸载还原、首启引导 | G5 卸载无残留 |

合计约 15 周。对比 v2.3 的「12 周做完 13 个模块」，这版少了 10 个模块、多了 3 周，是把时间还给质量。

---

## 八、风险

| 风险 | 影响 | 应对 |
|---|---|---|
| `SysListView32` 跨进程写入被 Windows 加固阻断 | **高，动摇主线一** | M2 spike 优先验证；失败则分区降级为「只画框 + 规则移动文件到子文件夹」，不控制图标坐标 |
| 壁纸合成方案在超宽/多屏/高刷下有闪烁 | 中 | spike 同期验证；备选切候选 B |
| Windows 更新改变 Z 序或 DefView 行为 | 中 | 桌面层抽象为独立模块 + G1–G6 每版本回归 |
| 杀软对跨进程内存写入误报 | 中 | 代码签名 + 白名单提交 + 该模块开源 |
| 又一次功能蔓延 | **高，是本文档存在的理由** | §3.3 不做清单 + §6 门槛前置；新功能提案必须先说明砍掉什么 |

---

## 九、与 v2.3 的关系

`zDesktop设计案.md`（v2.3）**不再是开发依据**，仅作为历史记录保留。其中仍然有效的部分已被本文吸收：视觉设计令牌（§三 设计系统）、竞品分析事实、桌面接管的 Z 序结论。其余章节（13 模块清单、16 页交互流程、里程碑、风险表）全部作废。

`pages/` 下 16 张 HTML 设计稿中，仅 `home.html`、`file-classify.html`、`desktop-widgets.html`、`automation-rules.html`、`settings.html` 5 张仍在范围内，其余归档。

---

*v3.0 核心主张：桌面增强产品的价值不在于功能条目数，而在于用户敢不敢让它开机自启。先把「零破坏」做成可验证的工程事实，再谈功能。*
