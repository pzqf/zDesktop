# zDesktop 设计案 v3.1 — 不接管的桌面增强层

> **版本**: v3.1 | **日期**: 2026-07-25 | **状态**: 待评审
>
> **本版取代 v2.3**（`zDesktop设计案.md` 保留作为历史存档，不再作为开发依据）。
>
> **v3.0 → v3.1 变更**：v3.0 定了方向但留了六个空洞，本版补齐——
> 1. 新增 **§四 图标位置真相源与同步模型**：主线一的核心机制，v3.0 完全没写。
> 2. 新增 **§五 数据模型**、**§六 首次运行与资产处置**、**§七 失败降级矩阵**。
> 3. **§八 性能预算**分解到状态级，取代 v2.3 那句没人测的「内存 < 150MB」。
> 4. **§十 验收门槛按可执行性分三层**——v3.0 把「虚拟显示器 + 截图断言」写成能进 CI，不现实。
> 5. **Plan B 展开**为完整方案，不再是风险表里的一句话。
> 6. 发布版本号改用 `0.x` 递增，1.0 留到成熟后。
>
> **v3.0 的方向性结论全部保留**：放弃自渲染图标层、13 模块砍到 3 主线、零破坏契约作为硬门槛。

---

## 零、为什么要重写

v2.3 是一份**功能清单**，不是产品定义。它列了 13 个模块、16 张设计稿、若干特性表格，但没有回答三个决定性问题：用户是谁、什么场景下打开它、为什么不用「Fences + Everything + PowerToys」这套免费组合。

代价已经在代码里兑现了。截至 v2.3 实现（约 1.3 万行 UI 代码、零测试）：

| v2.3 承诺 | 实现现状 |
|---|---|
| 核心原则「不破坏原生桌面」 | 默认隐藏原生图标层并自渲染，回收站/此电脑/副屏图标全部消失 |
| 壁纸引擎（DX11 + DirectComposition + H.265） | 仅 `SPI_SETDESKWALLPAPER` + 必应下载，零 GPU 代码 |
| 任务栏增强（整个模块 7） | 纯 UI mock，代码注释自述「不实际替换系统任务栏」 |
| 快速启动器（Alt+Space 悬浮） | Alt+Space 打开大设置窗口的搜索页，而非轻量启动器 |
| 高风险项对策（Z 序自愈 / 全屏检测 / 多屏 / DPI） | 四项全部未实现 |

根因是设计案把 90% 的交互笔墨给了「16 个页面之间怎么跳转」，于是实现把 90% 的代码给了那个 16 页的设置窗口。**但这个产品真正的界面是桌面本身、Alt+Space 和托盘，设置窗口只是配置面板。**

---

## 一、产品定位

**zDesktop 是 Windows 原生桌面的增强层——它在原生桌面之上叠加原生做不到的能力，而不替换原生的任何部分。**

一句话检验标准：**任何时刻杀掉 zDesktop，桌面必须与从未安装过完全一致。**

### 目标用户

桌面文件常年 50+ 个、习惯用桌面当工作台的 Windows 重度用户。他们已经知道 Fences 和 Everything 存在，但不想装四个软件、也不想为「一个画框工具」付订阅费。

**明确不是目标用户**：追求桌面视觉效果的美化党（他们要的是 Wallpaper Engine）、桌面常年只有 5 个图标的极简用户（他们没有痛点）、企业批量部署场景（需要域策略/静默配置，另一个产品）。

### 为什么用户要换

| 对手 | 它的问题 | zDesktop 的答案 |
|---|---|---|
| Fences | 收费、只有分区、无自动化 | 分区 + 组件 + 规则一体，本地免费 |
| 元气桌面 | 广告、云端账号、效率工具弱 | 无广告、无账号、本地优先 |
| 腾讯桌面整理 | 分类粗糙、隐私存疑 | 规则可编辑、全本地 |
| PowerToys | 不管桌面 | 桌面是主场 |
| 什么都不装 | 桌面越用越乱 | 规则一次配置，长期自动维持 |

**差异化落点：Windows 上没有 Hazel。**「文件落到桌面 → 按规则自动归入分区/移走」这条链路是本产品唯一没有成熟免费替代品的能力，也是长期护城河。

### 成功指标

功能做完不等于做成。以下三条决定这个产品是否成立：

| 指标 | 目标 | 为什么是它 |
|---|---|---|
| 安装 7 天后仍保持开机自启 | > 60% | 桌面工具的唯一真实留存信号；用户一旦觉得碍事就会关掉自启 |
| 因 zDesktop 导致的桌面异常报告 | **0** | 零破坏契约的外部验证。这是一票否决项 |
| 首次「一键整理」后 24 小时内撤销 | < 10% | 衡量规则默认值是否符合直觉；撤销率高说明自动分类不可信 |

---

## 二、第一原则：零破坏契约

本节优先级高于本文档其余全部内容。**违反本节的功能一律不做，无论它多有吸引力。**

1. **永不隐藏、永不替换 `SHELLDLL_DefView` / `SysListView32`。** 桌面图标始终由 Explorer 渲染和管理。回收站、此电脑、多选框选、F2 重命名、与资源管理器互拖、实时刷新、右键菜单——全部是 Explorer 自带的，我们一行都不重写。
2. **永不使用 `HWND_TOPMOST`** 作为常驻状态。覆盖层通过 Z 序锚定在图标层之上、任务栏之下。
3. **永不在用户未确认前移动文件或改变图标布局。** 首次整理、规则首次生效、批量操作，一律先预览后执行（见 §六）。
4. **所有对系统状态的修改必须可逆且有还原记录**：壁纸、图标位置、快捷方式图标，改前落盘原值，退出/崩溃/卸载三条路径都能还原。
5. **强杀即恢复。** 不依赖 `OnExit` 跑完。任何需要「下次启动时修复」的设计都不合格。
6. **全屏应用期间零存在感。** 检测到全屏/游戏模式，覆盖层隐藏、所有定时器停摆。

---

## 三、功能范围

### 3.1 三条主线

#### 主线一：桌面分区 —— P0

在**原生图标**之上做分组容器，而不是重画图标。核心机制见 §四，这里只定义用户可见行为。

| 能力 | 说明 |
|---|---|
| 创建分区 | 在桌面空白处按住鼠标右键拖出矩形 → 释放后弹出「新建分区」（Fences 的经典手势）；或从托盘菜单新建 |
| 分区外观 | 半透明圆角矩形 + 标题栏，可自定义名称、颜色、大小、位置 |
| 图标归属 | 图标被拖入分区矩形范围即归属该分区；拖出即解除归属 |
| 容器内排布 | 网格自动排列；可按名称/类型/修改时间排序；图标超出容器高度时容器内滚动 |
| 折叠 | 双击标题栏折叠为标题条，再双击展开 |
| 移动分区 | 拖动标题栏移动，组内图标整体跟随 |
| 自动入区规则 | 按扩展名 / 文件名正则 / 修改时间自动归入指定分区（规则引擎与主线三共用，见 §3.3 语义区分） |
| 一键整理 | 桌面所有未归属文件按规则入区；**执行前落盘快照，30 秒内可一键撤销，快照永久保留可随时回滚** |

#### 主线二：桌面组件 —— P0

覆盖层窗口承载浮动组件，位于图标层之上、非置顶、非组件区域鼠标透传。现有实现方向正确，需按 §八 硬化。

MVP 组件：时钟、日历、待办、天气、系统监控（均已有实现）。

组件特性：自由拖拽、网格吸附、S/M/L 尺寸、折叠为图标、每组件独立设置面板。

**组件与分区的空间关系**：组件在覆盖层（图标层之上），分区背景在图标层之下，两者可能视觉重叠。规则是——**组件拖动时，与分区矩形重叠超过 30% 则吸附到分区外缘**，避免遮挡分区内图标。分区新建/调整大小时同理避让已有组件。

**不做组件商店、不做付费组件、不做插件系统**（1.0 前）。

#### 主线三：自动化规则（Windows 版 Hazel）—— P1

产品的差异化核心。现有 `AutomationService`（`FileSystemWatcher` 实现）是全仓完成度最高的模块，直接演进。

| 能力 | 说明 |
|---|---|
| 监控范围 | 桌面 + 用户指定文件夹（下载、截图目录等） |
| 触发条件 | 扩展名、文件名正则、大小、创建/修改时间、在该文件夹中存在的时长 |
| 执行动作 | 移动、复制、重命名（支持 `{日期}` 变量）、归入分区、删除到回收站、运行命令 |
| 执行日志 | 成功/跳过/失败可追溯，单条可撤销 |
| 内置模板 | 「截图超过 7 天移入归档」「安装包用完即删」「按类型整理桌面」 |

**安全阀**（自动化动文件，出错代价最高，故单列）：

- 删除一律进回收站，**永不硬删除**，且不提供「跳过回收站」选项
- 新规则首次运行强制干跑，列出将影响的文件供确认后才真正启用
- 单次执行影响文件数超过 **50** 时暂停并请求确认，防正则写错导致全盘搬家
- `运行命令` 动作默认禁用，需在设置中显式开启并二次确认
- 所有已执行动作写入撤销日志，保留 30 天

**应用自动化（模拟键鼠）从范围内移除**——那是另一个产品，且触发杀软误报。

### 3.2 配套

- **`Alt+Space` 轻量启动器**（P1）：独立无边框小窗口，**绝不打开设置窗口**。应用/文件搜索（Everything 可用时走 Everything，否则走内置索引）、算式计算、网页搜索。全局搜索与快速启动器**合并为同一个东西**，不做两套。
- **边缘抽屉**（P2）：鼠标移至屏幕边缘呼出常用文件收纳区。
- **设置窗口**（P0，但降级）：从「产品主体」降为纯配置面板。页面从 16 砍到 6：概览、分区、组件、规则、外观、关于。

### 3.3 两种「规则」的语义区分

主线一和主线三共用规则引擎，但动作语义必须严格分开，否则用户会困惑于「整理」到底动没动文件：

| | 分区规则（主线一） | 自动化规则（主线三） |
|---|---|---|
| 作用 | 改变图标**归属与位置** | 改变文件的**物理位置** |
| 文件路径 | **不变**，文件仍在桌面根目录 | 改变，文件被移走 |
| 可逆性 | 完全可逆（只是坐标） | 需靠撤销日志 + 回收站 |
| 触发时机 | 新文件出现、手动整理 | 文件事件 + 时间条件 |
| UI 措辞 | 「归入分区」 | 「移动到…」 |

**共用的只有条件匹配器**，动作执行器是两套。

### 3.4 显式不做清单

写进设计案是为了防止复发。以下功能**不在 1.0 范围内，提案一律拒绝**：

| 不做 | 原因 |
|---|---|
| 任务栏替换 / Dock 栏 | 需系统级 hook，与零破坏契约冲突；打不过 BitDock |
| 动态壁纸引擎（DX11/视频/网页壁纸） | 无底洞，正面撞 Wallpaper Engine 和元气桌面 |
| 壁纸广场 / 壁纸内容生态 | 冷启动无内容，且不是我们的战场 |
| 图标着色 / 图标包 | 不再自渲染图标，着色失去载体（图标替换见下方备注） |
| 多窗格文件管理器（Q-Dir） | 与桌面增强层定位无关，是独立产品 |
| 窗口管理（分屏/置顶/透明度） | PowerToys FancyZones 免费且更好 |
| 鼠标手势 / 虚拟桌面 / 专注模式 | 锦上添花，稀释精力 |
| 剪贴板历史 | 隐私模型需单独设计，Win+V 已内置 |
| 控制中心（系统开关聚合） | Windows 11 已有，重复造轮子 |
| 云同步 / 账号体系 | 与「本地优先」定位冲突，且是长期运维负担 |

> 备注：**图标替换**若要做，唯一合规路线是改写桌面 `.lnk` 文件的图标指向（真实修改用户文件），必须配套备份 + 一键还原，且默认关闭。不通过隐藏原生层实现。

---

## 四、核心机制：图标位置真相源与同步模型

> **本节是主线一能否成立的关键，v3.0 缺失。** 分区的本质不是「画框」，而是「在不接管图标的前提下控制图标位置」，这需要一套明确的真相源与同步规则。

### 4.1 冲突的本质

Explorer 的 `SysListView32` 持有桌面图标坐标的权威副本，并且会在我们不知情时改写它：

| Explorer 的动作 | 后果 |
|---|---|
| 右键「自动排列图标」开启 | 所有图标被强制吸附到左上角网格，分区**完全失效** |
| 右键「按名称/大小/类型排序」 | 全部图标重排，分区归属的视觉表现被打乱 |
| 新文件出现 | Explorer 放到第一个空网格位，可能落在某分区框内但无归属 |
| 分辨率 / 缩放变化 | Explorer 自行重排图标以适应新工作区 |
| 用户手动拖动图标 | 坐标变化，归属可能需要更新，但**无事件通知** |

### 4.2 设计决策

**决策 1：zDesktop 是归属的真相源，Explorer 的坐标是渲染结果。**

持久化的不是绝对坐标，而是 `文件路径 → (分区 ID, 分区内序号)`。绝对坐标每次由「分区几何 + 序号 + 网格尺寸」实时计算得出。

这样做的收益：分辨率变化、缩放变化、分区移动、分区排序，全部只需重算坐标再写回，不依赖 Explorer 里那份易失的坐标。未归属图标的坐标则完全交给 Explorer，我们不碰。

**决策 2：「自动排列图标」必须关闭；「与网格对齐」不关，改为对齐 Shell 网格。**

> M2 spike 修订。原文只把「自动排列」列为前置条件，实测发现还有第二个约束。

Explorer 有两个会覆盖我们写入坐标的选项，性质不同：

| 选项 | 样式位 | 影响 | 处理 |
|---|---|---|---|
| 自动排列图标 | `LVS_AUTOARRANGE`（窗口样式） | 强制所有图标吸到左上角依次排列，分区**完全失效** | **必须关闭**，硬前置条件 |
| 将图标与网格对齐 | `LVS_EX_SNAPTOGRID`（**扩展**样式 `0x80000`） | 写入坐标被吸附到最近格点，实测 `(2030,98)` → `(2010,102)` | **不关**，改为按格点计算坐标 |

第二项是扩展样式，`GWL_STYLE` 里查不到，必须用 `LVM_GETEXTENDEDLISTVIEWSTYLE` 查询——
这正是 spike 最初误判为「写入失败」的原因。

**不强关吸附**：实测关掉后任意坐标确实能精确写入，但那是在改用户可见的桌面设置，属侵入。
改为用 `LVM_GETITEMSPACING` 运行时查询网格间距（本机实测 76×100 物理像素，
随 DPI 与图标大小变化，**不可硬编码**），分区内排布一律落在格点上。实测格点坐标可精确写入。

于是分区只剩「自动排列关闭」这一个前置条件。zDesktop 启动时检测，若开启则告知用户
「分区功能需要关闭桌面的『自动排列图标』」，提供一键关闭 + 记录原值（卸载还原）。
**用户拒绝则分区功能整体禁用**，其余功能照常，不做半残状态。

**决策 3：同步是双向的，但两个方向机制不同。**

```
zDesktop ──写──> Explorer     LVM_SETITEMPOSITION（事件驱动，我方主动）
                              触发：分区增删改、图标入区/出区、显示器变化、排序

zDesktop <──读── Explorer     LVM_GETITEMPOSITION（轮询，被动感知）
                              触发：仅在「桌面获得焦点」期间轮询
```

**决策 4：轮询只在桌面聚焦时进行。**

这是性能设计的关键。图标坐标只可能因用户拖拽而改变，而拖拽必然发生在桌面获得焦点期间。因此：

- 桌面**未**聚焦（用户在用别的程序）：**完全不轮询**，CPU 占用为 0
- 桌面聚焦：以 **2Hz** 轮询图标坐标，感知用户拖拽结果
- 失焦时做最后一次读取并落盘，然后停止

文件的增删改则完全由 `FileSystemWatcher` 事件驱动，无需轮询。

**决策 5：冲突以用户最后一次直接操作为准。**

用户手动把图标拖出分区矩形 → 归属更新为「无分区」，并且**该文件被标记为「用户手动放置」，后续自动规则不再动它**，直到用户显式重置。这条防止「我明明拖出来了，它又自己跑回去」这种最招人恨的行为。

### 4.3 分区背景的绘制

**已定：采用候选 A（合成进壁纸）。** M2 spike 完成，候选 B 不启用。

zDesktop 把「用户壁纸 + 分区半透明矩形」合成为一张图设为桌面壁纸。零 Z 序问题、
零常驻渲染开销、退出还原原壁纸即可。拖动分区时用临时交互层跟手，松手后才合成落盘。

实测合成耗时（解码 + 绘制 4 个圆角矩形 + JPEG 编码 + 落盘）：

| 分辨率 | 耗时 | 产物 |
|---|---|---|
| 1920×1080 | 10.1 ms | 51 KB |
| 2560×1440 | 17.8 ms | 82 KB |
| 3840×2160 | 37.8 ms | 165 KB |

4K 下 37.8ms 不可能每帧重合成，但按上述「松手才合成」的规定，
该耗时只发生一次，用户感知为一次性延迟而非掉帧，可接受。

未启用的候选 B（`DefView` 之下的独立背景窗口）视觉更灵活（可做实时模糊/动效），
但 Z 序维护复杂度高，保留为候选 A 失效时的后备。

**M3-C 实测修订两处**：

1. **改用 `IDesktopWallpaper`（Win8+ COM）而非 `SPI_SETDESKWALLPAPER`。**
   后者只能设「一张壁纸」，多屏下无法按屏分别设置、也读不出每屏当前用的是哪张图。
   实测本机摆放方式为 `Fill`（每屏独立），可按屏分别合成互不影响；
   若为 `Span`（跨屏一张）则须按虚拟屏尺寸整张出图。

2. **必须防「自我叠加」。** 若每次以「当前壁纸」为底图，我们自己的输出会成为
   下一次的底图，分区框被反复叠加。因此底图永远取**还原账本里记录的原图**，
   仅当当前壁纸不是我们的产物（按缓存目录前缀判定）时才更新该记录。

   这一条在真机上不是假想：实测本机壁纸由**元气桌面**管理
   （`E:\元气壁纸缓存\`）。它轮换壁纸时，我们以新壁纸为底图重新合成；
   我们合成后，它下次轮换又会覆盖 —— 两边都能正常工作，不会互相破坏。

**M3-D 实测：不存在淡入动画，但有约 0.5 秒的落地延迟。**

用高速连拍量化（松手后连拍 2.2 秒、约 28fps，测相邻帧平均亮度差）：

| 指标 | 实测 |
|---|---|
| 最大帧间亮度差 | 4.74（0-255 标度，约 1.9%） |
| 可见跳变帧数 | **1 帧**，跨度 0 ms |
| 松手到跳变的延迟 | 643 ms（去抖 350ms 时）→ **496 ms**（去抖降到 120ms 后） |

结论两条：

1. **Windows 没有走淡入过渡** —— 只跳变 1 帧，是一次性切换。原先担心的
   「淡入闪烁」不存在。
2. **真正的问题是延迟突跳**。去抖从 350ms 砍到 120ms 只兑现了约 150ms，
   剩下约 0.4 秒是 `SetWallpaper` 到 Windows 重绘桌面的耗时，**应用侧无法再压缩**。
   拖动时旧底色留在原处、只有标题栏在动，半秒后才「啪」地跳到新位置 ——
   用户报告的「可感知闪烁」实为此两段式观感。

**缓解措施（已实施）**：拖拽期间在覆盖层里把新位置**实心**画出来，
视线全程有着落，壁纸随后跟上时不再突兀。

**这约 0.5 秒是候选 A 的固有代价。**

#### 候选 B 已实测证伪（Windows 11）

用户反馈 0.5 秒延迟不可接受后，完整试做了候选 B —— **在本机不可行**。

**前置条件全部满足**（探测工具 `spikes/M3-WorkerWProbe`）：

| 检查项 | 结果 |
|---|---|
| 向 Progman 发 `0x052C` 后能否拿到壁纸层 WorkerW | ✅ 能（Win11 下它是 Progman 的**子窗口**，非顶层兄弟） |
| Z 序是否在图标层之下 | ✅ 是（DefView index=0，WorkerW index=1） |
| 是否已被第三方占用 | ✅ 未被占用（本机的元气桌面走设壁纸文件那条路，不抢这层） |
| 矩形是否等于虚拟屏 | ✅ 恰好相等，与 ListView 客户区同坐标系 |
| 窗口能否寄生进去 | ✅ `HwndSource` 寄生成功（日志确认） |

**但画面始终不出现。** 四种渲染方式全部失败：

| 方式 | 结果 |
|---|---|
| WPF `Window` + `AllowsTransparency=true` | 什么都不显示（该机制依赖顶层分层窗口） |
| `WS_EX_LAYERED` + 色键 | `SetLayeredWindowAttributes` 返回 87，扩展样式确认已置位（0x080000A0） |
| 不透明 + 补 `WS_CHILD` | 仍不渲染（样式 0x46080000） |
| `HwndSource` + `WS_CHILD` 直接建子窗口 | 仍不渲染 |

**结论**：Win11 的壁纸不再由该 WorkerW 绘制，而是 DWM 直接合成在它**之上**，
放进去的内容会被壁纸盖住。经典 WorkerW 技巧在新版 Win11 上已失效
（Lively 等项目亦有同类报告）。

代码保留并加开关 `FenceController.EnableWallpaperLayerRendering`（默认关闭）：
Win10 与旧版 Win11 仍适用该拓扑，将来可按系统版本条件启用。

**现状**：候选 A 为唯一可用方案，0.5 秒延迟暂时无解。若要彻底消除，
需要的是「自绘整个桌面壁纸层」级别的方案（Wallpaper Engine 路线），
那已超出「不接管的桌面增强层」这一产品定位，**不做**。

---

## 五、数据模型与持久化

所有数据落在 `%APPDATA%\zDesktop\`，纯本地，无云同步。

| 文件 | 内容 | 备注 |
|---|---|---|
| `fences.json` | 分区定义 + 图标归属 | 见下方结构 |
| `layout.json` | 组件布局 | 已有 |
| `rules.db` (SQLite) | 自动化规则、执行日志、撤销日志 | 日志量大，用 SQLite 而非 JSON |
| `snapshots/` | 整理前的桌面快照 | 每次批量操作一份，保留最近 20 份 |
| `restore.json` | 安装时的系统原值（壁纸路径、自动排列开关状态等） | 卸载还原用 |

**`fences.json` 结构要点**：

```jsonc
{
  "version": 1,
  "fences": [
    {
      "id": "f-01",                    // 稳定 ID，重命名不影响归属
      "monitorKey": "DISPLAY1#...",    // 显示器稳定标识，非索引
      "name": "工作", "color": "#6c5ce7",
      "rect": { "x": 40, "y": 60, "w": 420, "h": 300 },  // DIP，相对该显示器工作区左上角
      "collapsed": false,
      "sortMode": "name",
      "rules": [ { "kind": "ext", "value": [".docx", ".xlsx"] } ]
    }
  ],
  "assignments": [
    { "path": "C:\\Users\\x\\Desktop\\报告.docx", "fenceId": "f-01", "order": 3, "manual": false }
  ]
}
```

**三个必须处理的持久化问题**：

1. **路径会变。** 文件重命名/移动后 `assignment` 失效。用 `FileSystemWatcher` 的 `Renamed` 事件同步更新路径；进程未运行期间发生的重命名无法追踪，启动时清理指向不存在文件的孤儿记录。
2. **坐标必须是相对的。** `rect` 存相对该显示器工作区的 DIP，不存全局物理像素——否则换分辨率、换缩放、换主屏全部错位。
3. **显示器会拔掉。** `monitorKey` 用设备稳定标识而非索引（索引会因插拔顺序变化）。显示器移除时分区**不删除**，标记为孤儿并隐藏；重新插上自动恢复。设置里提供「把孤儿分区迁移到主屏」。

---

## 六、首次运行与用户资产处置

> v3.0 完全没写这一节，但它直接决定「安装 7 天后仍开机自启」这个成功指标。

### 6.1 首次运行

**原则：装上之后，在用户点「应用」之前，桌面一个像素都不许变。**

```
安装完成 → 托盘出现图标 + 一张引导卡片（右下角，非模态，可直接关掉）
           「zDesktop 已在后台运行。你的桌面有 47 个文件，要试试自动整理吗？」
                    [ 预览效果 ]   [ 以后再说 ]
                              │
                    点「预览效果」
                              ↓
           预览态：桌面上以半透明虚线框显示「将创建 4 个分区」，
                   图标显示将要移动到的位置（动画预演，不实际写入）
                   底部条：「将创建 4 个分区，重新排布 37 个图标」
                    [ 应用 ]   [ 调整分区 ]   [ 取消 ]
                              │
                          点「应用」
                              ↓
           落盘快照 → 执行 → toast：「已整理。30 秒内可撤销」[ 撤销 ]
```

「以后再说」之后不再弹第二次。用户随时可从托盘进入。

### 6.2 卸载与资产处置

**这是最容易伤害用户、也最容易被忽略的一环。** 分区一旦消失，桌面上几十个图标会散落一地，用户会觉得「这软件把我桌面搞乱了」——即使技术上我们什么都没删。

卸载时必须询问（默认选第一项）：

| 选项 | 行为 |
|---|---|
| **保持现状**（默认） | 图标保留在分区形成的位置上，只是分区框消失。桌面看起来仍然整齐 |
| **收进文件夹** | 每个分区在桌面上创建一个同名真实文件夹，把该分区的文件移进去。桌面只剩几个文件夹 |
| **恢复安装前布局** | 用 `restore.json` 里安装时的快照还原图标位置 |

无论选哪项，都必须还原：壁纸（若被分区合成改写）、「自动排列图标」开关原值、开机自启项、`%APPDATA%\zDesktop\`。

---

## 七、失败降级矩阵

> 设计一个常驻后台、会改桌面的软件，**失败时的行为比成功时的行为更重要**。以下每一条都必须有确定的降级态，不允许「未定义行为」。

| 失败场景 | 检测方式 | 降级行为 | 用户可见反馈 |
|---|---|---|---|
| `LVM_SETITEMPOSITION` 写入被系统阻断 | 写入后回读校验坐标 | 分区功能整体禁用，转 Plan B（§十二），组件/规则不受影响 | 托盘气泡一次性提示 + 设置页说明 |
| 「自动排列图标」被用户或第三方重新打开 | 启动时 + 桌面聚焦时检查注册表 | 分区暂停生效（框还在，不再纠正坐标） | 分区标题栏显示警告图标，点击可一键关闭该选项 |
| Explorer 崩溃 / 重启 | `TaskbarCreated` 广播消息 | 重新查找 `DefView`、重建覆盖层、重锚 Z 序、按归属重写坐标 | 无（应当无感） |
| 桌面图标层 HWND 失效 | `IsWindow` 校验（已实现，5s 兜底） | 重新查找并重锚 | 无 |
| 壁纸合成失败（磁盘满 / 权限 / 图片损坏） | 合成异常捕获 | 保留用户原壁纸，分区改用候选 B 或无背景（仅标题栏） | 设置页提示 |
| 第三方工具改了壁纸 | 监听 `WM_SETTINGCHANGE` | 以第三方壁纸为新底图，重新合成分区背景 | 无 |
| 显示器移除 | `WM_DISPLAYCHANGE` | 该屏分区标记孤儿并隐藏，不删除数据 | 设置页显示孤儿分区，可迁移 |
| 规则匹配数异常（> 50） | 执行前计数 | 暂停执行，等待用户确认 | 模态确认框，列出受影响文件 |
| 配置文件损坏 | 反序列化失败 | 备份损坏文件为 `.bak`，以默认配置启动 | 托盘提示，指向备份文件 |
| zDesktop 自身崩溃 | `CrashGuard` | 还原原生图标层可见性、还原壁纸、清运行标记 | 下次启动时告知上次异常退出 |

---

## 八、技术架构与性能预算

```
┌──────────────────────────────────────────────────────────────┐
│  Explorer（不属于 zDesktop，我们只读写它，不替换它）             │
│  桌面壁纸  <  SHELLDLL_DefView / SysListView32（原生图标）      │
└──────────────────────────────────────────────────────────────┘
                            ▲                    ▲
             图标位置读写（LVM_*，见 §四）  分区背景合成进壁纸
                            │                    │
┌──────────────────────────────────────────────────────────────┐
│  zDesktop.Shell.Fences                                        │
│  IconPositionSync（真相源同步：焦点驱动轮询 + 事件写回）        │
│  FenceLayout（容器模型 / 归属计算 / 坐标解算）                  │
│  FenceCompositor（背景合成）                                   │
├──────────────────────────────────────────────────────────────┤
│  zDesktop.Shell.Desktop — 覆盖层                              │
│  OverlayWindow（DefView 之上，非置顶，hit-test 透传）          │
│  ZOrderKeeper（WM_WINDOWPOSCHANGING 自愈）        ✅ 已实现    │
│  MonitorSet（多屏：每屏一个 overlay 实例）                     │
│  FullscreenGuard（SHQueryUserNotificationState）              │
│  SafetyNet（崩溃 / 强杀 / 卸载三路还原）                        │
├──────────────────────────────────────────────────────────────┤
│  功能服务（与桌面层解耦，可独立单测）                            │
│  Automation（规则引擎 + 干跑 + 撤销日志）  Widgets（组件宿主）   │
│  Launcher（Alt+Space）   Search（Everything / 内置索引）        │
├──────────────────────────────────────────────────────────────┤
│  Data：见 §五                                                  │
└──────────────────────────────────────────────────────────────┘
```

### 技术选型

沿用 WPF (.NET 8)，补三项 v2.3 缺失的基础设施：

| 项 | 决定 |
|---|---|
| DPI | 新增 `app.manifest` 显式声明 `PerMonitorV2`；所有 Win32 像素 ↔ DIP 换算统一走 `DpiHelper`，禁止在业务代码里手写换算 |
| 多屏 | `EnumDisplayMonitors` 枚举，**每个显示器一个 overlay 实例**；监听 `WM_DISPLAYCHANGE` + `WM_DPICHANGED` 重建 |
| UI 写法 | 新增页面一律 XAML + 数据绑定；存量代码页禁止继续膨胀（见 §九） |
| 测试 | 新增 `zDesktop.Tests`（xUnit）+ `zDesktop.SystemTests`（门槛回归，见 §十） |

### 性能预算

取代 v2.3 那句没人测的「内存 < 150MB / CPU < 1%」。按**运行状态**分解，每条都可被 §十 的 T1/T2 验证：

| 状态 | CPU | 活动定时器 | 说明 |
|---|---|---|---|
| 全屏应用运行中 | **0%** | 0 | 覆盖层隐藏，全部定时器停摆 |
| 空闲（桌面未聚焦） | **< 0.1%** | 仅 Z 序兜底（5s） | 无图标轮询；文件变化由 `FileSystemWatcher` 事件驱动 |
| 桌面聚焦 | **< 1%** | Z 序 5s + 图标坐标 2Hz | 见 §4.2 决策 4 |
| 拖动分区中 | < 5%（瞬时） | 跟手渲染 | 松手后回落 |

**CPU 数字是门槛不是目标**：超标即视为缺陷，不允许以「功能需要」为由放宽。

#### 内存预算（v3.1 修订，基于实测）

v3.0 写的「常驻工作集 < 120MB（WPF 基线约 60MB）」是**未经测量的臆断，且不可达**。
M1 实测数据（Win11 / 32 逻辑核 / .NET 8 / Release）：

| 被测对象 | 工作集 | 私有字节 |
|---|---|---|
| 空 WPF 窗口（什么都不做） | 108.6 MB | 59.8 MB |
| 空 WPF + WinForms 托盘图标 | 112.3 MB | 60.7 MB |
| zDesktop（M1，已执行 §九 删减） | 179.5 MB | 129.2 MB |

结论三条：

1. **空 WPF 应用的工作集地板就是 108MB**，「< 120MB」只给整个应用留了 11MB，
   由构造即不可能满足。原数字作废。
2. **改用私有字节作为门槛**。工作集包含框架共享页，且随系统内存压力浮动，
   同一份代码在不同机器上能差几十 MB —— 它不适合做门禁指标。
3. 已排除的三个嫌疑：删掉 24 万字符代码（§九）只省 4MB；GC 调参
   （`gcConcurrent` / `GCConserveMemory`）**完全无效**；关闭后台应用/文件索引省 1.5MB。
   即我们自己那约 69MB 是摊在覆盖层、组件、主题、JIT 上的，**没有单点热区**，
   不存在「改一处省一大块」的优化。

修订后的门槛：

| 指标 | 门槛 | 说明 |
|---|---|---|
| 私有字节（常驻） | **< 160 MB** | 当前 129MB，留约 30MB 给 M3 分区 |
| 相对空 WPF 基线的增量 | **< 90 MB** | 跨机器可比，剔除框架地板差异 |
| 每个组件增量 | < 5 MB | 组件是可增长项，需单独盯 |
| 工作集 | 仅记录，不作门禁 | 受系统内存压力影响，不可作为跨机器判据 |

**若 M3 分区落地后突破 160MB**，先按上表定位是哪一项增长，再决定优化还是调整门槛——
但调整门槛必须像本节一样附上实测数据，不允许再出现拍脑袋的数字。

---

## 九、存量代码处置

| 模块 | 处置 | 理由 |
|---|---|---|
| `AutomationService`（34KB） | **保留并演进** | 全仓质量最高，是主线三的底座 |
| `WidgetHost` / `WidgetContainer` / 7 个 Widget | **保留并硬化** | 主线二，方向正确 |
| `DesktopOverlayWindow` | **保留，核心已部分重写** | Z 序自愈与 DPI 换算已完成；多屏、全屏检测待 M1 |
| `EverythingBridge` / `FileIndexService` | **保留** | 启动器底座 |
| `Theme` / `LayoutStore` / `TrayIconManager` | **保留** | — |
| `WallpaperService` | **保留并升级** | 成为分区背景合成的底座 |
| `MainWindow` + 13 个 Page | **大幅裁剪** | 16 页 → 6 页；砍掉的页删除而非注释 |
| `DesktopIconLayer` / `DesktopIconItem` / `DesktopIconStore` | **删除** | 自渲染图标层，与零破坏契约冲突（M0 已默认关闭，M3 随分区落地删除） |
| `IconManagerService`（36KB） | **删除** | 着色依附于自渲染图标层，载体已不存在 |
| `TaskbarPage`（25KB，纯 mock） | **删除** | 不做任务栏 |
| `DiskMapperService` + `DiskMapperPage`（65KB） | **移出主线**，归档到 `experiments/` | 独立产品，不属于桌面增强层 |
| `WindowManagerService` + Page（35KB） | **删除** | 见不做清单 |
| `ControlCenterService` + Page（68KB） | **删除** | 见不做清单 |
| `GlobalSearchPage` + `QuickLaunchPage`（70KB） | **合并重写**为轻量启动器窗口 | 两套变一套，且不再寄生在设置窗口里 |

净效果：删除约 25 万字符 UI 代码，主线代码占比从约 20% 提升到 70% 以上。

> **删除前置条件**：仓库已有 git baseline 且已推送远端（已完成，`4594443`）。所有删除走单独提交，便于回溯。

---

## 十、验收门槛

> v3.0 把六条门槛都写成「自动化回归」，但其中「虚拟显示器 + 截图断言」「启动全屏窗口采样」在 CI 上并不现实。本版按**可执行性分三层**，每层定清楚频率和责任人，避免写了跑不动最后全部形同虚设。

### T1 — CI 自动化（每次提交，GitHub Actions）

纯逻辑，无需真实桌面环境。工作流见 `.github/workflows/ci.yml`（Windows runner，
`-warnaserror` 构建 + `dotnet test`）。

| # | 内容 | 状态 |
|---|---|---|
| T1-1 | DPI 换算：物理 ↔ DIP 在 100%/125%/150%/175% 下往返一致；矩形原点与尺寸同时换算；非法 DPI 退化为不换算 | ✅ M1 |
| T1-5 | 配置读写：v1→v4 版本迁移、损坏文件降级、往返一致；还原账本「先记账后动手」不变量 | ✅ M1 |
| T1-1b | 多屏：工作区按各屏自身 DPI 换算；显示器 key 非空且唯一（组件归属的持久化依据） | ✅ M1 |
| T1-2 | 分区坐标解算：槽位逐列填充、全部落在格点上、分区变窄重新折行、网格间距变化整体重算、极小分区不产生负尺寸；含复现 spike 实测吸附偏移的用例 | ✅ M3 |
| T1-3 | 归属模型：入区/出区/手动标记/孤儿清理/重命名跟随/分区删除后手动归属降级保留；规则匹配含非法正则不抛异常 | ✅ M3 |
| T1-4 | 规则引擎干跑：给定文件集与规则，匹配结果符合预期；> 50 命中时触发暂停 | ⏳ 随 M6 |
| T1-6 | 撤销日志：快照落盘/深拷贝/倒序列举/超限清理/损坏跳过；归属整体替换而非合并 | ✅ M3（分区部分）／⏳ M6 补规则日志 |

标 ⏳ 的用例依赖尚未实现的功能，**随对应里程碑一并交付，不得跳过**。

### T2 — 本地真机脚本（每次发版，开发者机器一键跑）

需要真实 Windows 桌面，但可脚本化断言：

| # | 内容 |
|---|---|
| T2-1 | 强杀恢复：`taskkill /F` 后校验壁纸、「自动排列」开关、图标层可见性全部还原 |
| T2-2 | Explorer 重启：结束 `explorer.exe` 后覆盖层自动重附着，Z 序关系正确 |
| T2-3 | 卸载无残留：卸载后对比环境快照，无遗留注册表项/文件/自启项 |
| T2-4 | 性能预算：四种状态下采样 CPU 与工作集，对照 §八 表格 |
| T2-5 | 显示器热插拔：模拟 `WM_DISPLAYCHANGE`，分区与组件不丢失、不重叠 |

### T3 — 人工验收清单（每个 Windows 大版本 + 每次发版）

只能靠眼睛，但必须有清单、有记录，不允许「大概看了下」：

| # | 内容 |
|---|---|
| T3-1 | 双屏混合 DPI（100% + 150%）下分区与组件视觉正确、跨屏拖动正常 |
| T3-2 | 全屏游戏中覆盖层不可见、无输入干扰 |
| T3-3 | 首次运行引导全流程（预览 → 应用 → 撤销）观感正确 |
| T3-4 | 与 Fences / Wallpaper Engine 同时运行时的行为 |
| T3-5 | 主流杀软（Defender / 火绒 / 360）不告警 |

**规则**：T1 红 → 阻断合入；T2 红 → 阻断发版；T3 红 → 记录为已知问题并评估是否阻断。

---

## 十一、里程碑

发布版本号采用 `0.x` 递增，**1.0 留到产品成熟后再发**，避免过早承诺稳定性。

| 阶段 | 周期 | 交付 | 出口标准 |
|---|---|---|---|
| **M0 止损** | ✅ 已完成 | 默认关闭自渲染图标层、DPI 换算修复、Z 序自愈、图标布局误清空修复 | 编译通过 + 默认启动不改桌面 |
| **M1 零破坏地基** | ✅ 已完成 | 多屏 overlay、`PerMonitorV2`、全屏让位、Explorer 重启自愈、还原账本 + `--restore`、§九 存量删减、T1（43 项）+ T2 脚本 + CI | **T1 全绿 ✅　T2 全绿 ✅**（12 通过 / 2 SKIP） |
| **M2 分区 spike** | ✅ 已完成 | 验证 `LVM_*` 跨进程读写 + 背景合成两条候选 + 「自动排列」检测 | **Plan A 可行（10/10）**，定候选 A；结论见 `spikes/M2-IconControl/结论.md` |
| **M3 分区落地** → `0.1` | ✅ 已完成 | 分区 CRUD、编辑模式拖拽新建、归属同步（§四）、排序、折叠、一键整理 + 快照撤销、背景合成 | T1 137 项全绿 ✅　T2 12 通过 0 失败 ✅ |
| **M4 首次运行体验** → `0.2` | ✅ 代码完成 | 引导卡片、预览态、撤销 toast、卸载资产处置三选项 + `--uninstall-cleanup` 入口 | T1 161 项绿；**预览→应用全流程待人工验收（T3-3）** |
| **M5 组件打磨** → `0.3` | 🔄 进行中 | 与分区的避让规则 ✅（含组件互不叠压）；5 个 MVP 组件与设置面板的打磨待做 | 多屏/DPI 下正确 |
| **M6 自动化规则** → `0.4` | 2 周 | 规则编辑器、干跑、安全阀、撤销日志、3 个内置模板 | 干跑测试集零误伤 |
| **M7 启动器** → `0.5` | 2 周 | `Alt+Space` 轻量窗口、Everything 集成 + 降级、算式 | 冷启动呼出 < 150ms |
| **0.9 发布候选** | 2 周 | 安装包（Inno Setup）、代码签名、卸载还原、稳定性回归 | T1/T2/T3 全绿 |

合计约 15 周。相比 v2.3 的「12 周做完 13 个模块」，这版少了 10 个模块、多了 3 周——把时间还给质量。

---

## 十二、风险与 Plan B

### 风险表

| 风险 | 影响 | 应对 |
|---|---|---|
| ~~`LVM_SETITEMPOSITION` 跨进程写入被 Windows 加固阻断~~ | ~~高~~ → **已排除** | M2 spike 实测通过：非管理员权限下 `OpenProcess` + `VirtualAllocEx` + `LVM_SETITEMPOSITION32` 全部成功，批量写入 5/5 生效 |
| 用户不肯关「自动排列图标」 | 中 | 引导中说明理由；拒绝则分区整体禁用，其余功能照常 |
| 索引→路径映射歧义 | 中 | `LVM_GETITEMTEXTW` 返回显示名，隐藏扩展名时同名不同类型的文件无法区分；M3 需结合 `IShellFolder` 或目录枚举消歧 |
| 壁纸合成在超宽/多屏/高刷下闪烁 | 中 | 合成耗时已实测（4K 37.8ms，仅松手时一次）；淡入动画的观感待 M3 真实分区后判断，必要时切候选 B |
| Windows 更新改变 Z 序或 `DefView` 行为 | 中 | 桌面层抽象为独立模块 + T2/T3 每版本回归 |
| 杀软对跨进程内存写入误报 | 中 | 代码签名 + 白名单提交 + 该模块开源；T3-5 覆盖 |
| 焦点驱动轮询漏掉某些坐标变化 | 中 | 桌面失焦时补一次全量读取；分区激活时强制重算 |
| 又一次功能蔓延 | **高，是本文档存在的理由** | §3.4 不做清单 + §十 门槛前置；新功能提案必须先说明砍掉什么 |

### Plan B：文件夹式分区

**触发条件**：M2 spike 证明无法可靠控制原生图标坐标。

**方案**：分区不再是「框住原生图标」，而是**桌面上一个真实文件夹的可视化门户**。

- 每个分区对应桌面上一个真实文件夹（如 `桌面\工作\`）
- 归入分区 = 文件被真实移动进该文件夹（不再是改坐标）
- zDesktop 在覆盖层中把该文件夹的内容渲染为一个可交互面板：图标列表、双击打开、拖入拖出、右键菜单（复用现有 `ShellContextMenu`）
- 原生桌面图标层完全不受影响——文件进了子文件夹，本来就不该在桌面根目录显示

**代价与收益**：

| | Plan A（控制原生坐标） | Plan B（文件夹门户） |
|---|---|---|
| 文件路径 | 不变，仍在桌面根 | **改变**，进入子文件夹 |
| 外部引用/脚本 | 不受影响 | 可能失效 |
| 图标交互 | 100% 原生 | 分区内由我们渲染（范围受限） |
| 技术风险 | 高（依赖未文档化的跨进程操作） | 低（纯文件操作 + 自有窗口） |
| 零破坏契约 | 满足 | 满足（未入区的图标仍是原生的） |

Plan B 的自渲染范围**仅限用户主动放入分区的文件**，与 v2.3 那种「接管整个桌面」有本质区别，因此不违反零破坏契约。但它改变文件路径，必须在首次运行引导中明确告知。

**判定**：Plan A 是首选，因为「文件路径不变」对重度用户价值很高。M2 spike 两周内给出结论，不允许悬而不决。

---

## 十三、与 v2.3 的关系

`zDesktop设计案.md`（v2.3）**不再是开发依据**，仅作为历史记录保留。其中仍然有效的部分已被本文吸收：视觉设计令牌、竞品分析事实、桌面接管的 Z 序结论。其余章节（13 模块清单、16 页交互流程、里程碑、风险表）全部作废。

`pages/` 下 16 张 HTML 设计稿中，仅 `home.html`、`file-classify.html`、`desktop-widgets.html`、`automation-rules.html`、`settings.html` 5 张仍在范围内，其余归档。

---

*v3.1 核心主张：桌面增强产品的价值不在于功能条目数，而在于用户敢不敢让它开机自启。先把「零破坏」做成可验证的工程事实，再谈功能。*
