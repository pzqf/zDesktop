<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>m3-syncverify</AssemblyName>
    <RootNamespace>ZDesktop.Spikes.M3Sync</RootNamespace>
    <PlatformTarget>x64</PlatformTarget>
    <UseWPF>true</UseWPF>
    <OutputPath>$(MSBuildThisFileDirectory)bin\$(Configuration)\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <ItemGroup>
    <!-- 刻意引用产品工程本身：这里要验的是真实代码路径，不是它的复制品 -->
    <ProjectReference Include="..\..\src\zDesktop.Shell\zDesktop.Shell.csproj" />
    <ProjectReference Include="..\..\src\zDesktop.Core\zDesktop.Core.csproj" />
  </ItemGroup>

</Project>
